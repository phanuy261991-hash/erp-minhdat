# Trạng thái hiện tại

**Cập nhật lần cuối**: 2026-08-07

## Giai đoạn

- ✅ **Sửa lỗi hiển thị: cột "Giá vốn" trong danh sách Sản phẩm luôn hiện `0` dù trang chi tiết hiện đúng giá vốn (2026-08-07, ngoài phase, theo phản hồi người dùng)**: **Không phải lỗi tính toán** — 2 nơi hiển thị cùng nhãn "Giá vốn" nhưng lấy từ 2 nguồn dữ liệu khác nhau. Danh sách (`products.js#renderRow()`) trước đây hiện `products.cost_price` — cột "giá vốn tham chiếu" nhập tay **không bắt buộc** trên form Thêm/Sửa sản phẩm, mặc định `0` và không bao giờ tự cập nhật theo lịch sử nhập kho. Trang chi tiết hiện `current_cost` — tính on-the-fly từ `stock_lots` qua `getWeightedAverageCost()` (`costing.service.js`), tự động đúng theo tồn kho thật. Vì hầu như không ai gõ tay "Giá vốn" lúc tạo sản phẩm, danh sách gần như luôn hiện `0` dù sản phẩm đã có giao dịch nhập/xuất thật. Đã hỏi qua `AskUserQuestion` — người dùng chọn **đổi cột danh sách sang hiển thị giá vốn thực tế** (giống trang chi tiết) thay vì đổi nhãn hoặc bỏ ô nhập tay. **Sửa**: `GET /api/products` (danh sách) nay trả kèm `current_cost` cho từng sản phẩm (dùng lại đúng `getWeightedAverageCost()` có sẵn, không viết lại công thức bằng SQL để tránh 2 nguồn tính trùng lặp dễ lệch nhau sau này); `products.js` đổi cột "Giá vốn" sang đọc `product.current_cost`. **Không đổi** ô "Giá vốn" nhập tay trên form Thêm/Sửa (vẫn dùng `cost_price` như cũ, đúng phạm vi người dùng chọn) và **chưa đụng** cột "Giá vốn" ở chức năng Xuất Excel (`POST /products/export`, vẫn dùng `cost_price` gốc — cùng lỗi hiển thị nhưng ngoài phạm vi câu hỏi đã hỏi, cần hỏi riêng nếu muốn sửa luôn). Test qua truy vấn DB trực tiếp + API thật (`GET /api/products`, tài khoản `admin`) + trình duyệt thật (Chrome headless CDP thô, đăng nhập bằng cookie session thật): xác nhận đúng kịch bản lỗi gốc (SP020: `cost_price=0` nhưng `current_cost=45.000` — danh sách trước sửa hiện `0`, sau sửa hiện đúng `45.000`), không lỗi console, không hồi quy tìm kiếm/sắp xếp (chỉ dùng `stock`, không đụng `cost_price`). Đã restart server sau khi sửa backend (bắt buộc, không có hot-reload).
- ✅ **Báo cáo: thêm bộ lọc kỳ (theo tháng/khoảng ngày) cho thẻ "Tổng số tiền đã thu", đổi thẻ "Dự án vượt dự toán vật tư" (2026-08-07, ngoài phase, theo yêu cầu người dùng)**: đã hỏi 2 câu qua `AskUserQuestion` trước khi code (bộ lọc chỉ áp dụng cho thẻ mới, 3 thẻ còn lại của section giữ nguyên số dư hiện tại; mốc "đã thu" tính theo ngày **thực sự** thu tiền, không phải ngày hẹn thu). Thẻ mới **"Tổng số tiền đã thu"** = tổng `amount` của các **đợt thanh toán đã thu đủ** (`status='da_thu_du'`, cùng điều kiện `computeMilestoneStatus()` đang dùng ở tab "Thanh toán & Công nợ") **và** thời điểm khoản trả cuối cùng làm đủ tiền (không phải lần trả đầu, không phải due_date) rơi vào đúng kỳ đang lọc — 1 đợt trả làm nhiều lần khác tháng chỉ tính vào đúng tháng có lần trả **hoàn tất**. `backend/routes/reports.routes.js`: thêm `monthBoundsUtc()`/`dateRangeBoundsUtc()` (giờ VN cố định UTC+7, cùng nguyên tắc `cashVoucher.service.js` — duplicate constant theo đúng tiền lệ dự án, không cross-require giữa 2 service) + `getTotalCollectedInPeriod()` (dò cumulative từng đợt thanh toán theo đúng thứ tự `created_at`). `GET /reports/projects` nhận `?month=YYYY-MM` hoặc `?from=&to=`, mặc định tháng hiện tại (VN) nếu không truyền gì. Frontend: bộ chuyển đổi "Theo tháng"/"Khoảng ngày" (2 nút, tái dùng pattern `.month-select`/`.filter-select` có sẵn, không dùng `<input type="month">` theo đúng quyết định cũ), auto-tải lại khi đổi lựa chọn. Test qua service trực tiếp (dữ liệu test riêng: 1 đợt trả đủ 1 lần trong tháng, 1 đợt trả 2 lần khác tháng — xác nhận chỉ tính vào tháng có lần trả hoàn tất) + qua trình duyệt thật (Chrome headless CDP thô). **Phát hiện lỗi trong chính kịch bản test đầu** (không phải lỗi ứng dụng): viewport mặc định của Chrome headless quá thấp khiến nút cần click nằm ngoài vùng nhìn thấy (`elementFromPoint` trả `null`), click qua CDP tại tọa độ đó không có tác dụng — sửa bằng cách đặt `--window-size` lớn hơn + `scrollIntoView()` trước khi bấm, xác nhận lại đúng cả 2 chiều chuyển đổi. Đối chiếu số liệu thật (120.000.000đ) khớp đúng 100% khi tính tay lại từ `debt_ledger`/`project_payment_milestones` (7 đợt thanh toán đã thu đủ). Không lỗi console. Đã restart server (bắt buộc, backend không có hot-reload).
- ✅ **Báo cáo: đổi thẻ "Trễ tiến độ" thành "Tổng giá trị hợp đồng" ở section "Dự án đang hoạt động" (2026-08-07, ngoài phase, theo yêu cầu người dùng)**: đã hỏi 2 câu qua `AskUserQuestion` trước khi làm (phạm vi tính: chỉ dự án đang hoạt động, khớp đúng 3 thẻ còn lại trong section — không tính Hoàn thành/Hủy; giá trị lấy: `contract_value` gốc, không cộng phát sinh đã duyệt). Backend `GET /api/reports/projects` (`backend/routes/reports.routes.js`) thêm `pr.contract_value` vào SELECT + `total_contract_value` (tổng contract_value của các dự án đang hoạt động) vào `summary`, bỏ hẳn `late_count` (không còn nơi nào dùng sau khi đổi thẻ). Frontend `reports.js` đổi `statCard('Trễ tiến độ', ...)` thành `statCard('Tổng giá trị hợp đồng', ...)`. **Nhãn "Trễ tiến độ" ở từng dòng trong bảng chi tiết (`renderProjectDelayCell()`) giữ nguyên, không đổi gì** — chỉ thay đúng 1 thẻ tổng hợp ở đầu trang. Test qua API (khớp đúng tổng tính tay từ DB: 500.000.000đ từ 3 dự án đang hoạt động) + trình duyệt thật (Chrome headless CDP thô): thẻ hiển thị đúng vị trí/nội dung, badge "Trễ" ở bảng chi tiết vẫn hoạt động bình thường, không lỗi console. Đã restart server sau khi sửa backend.
- ✅ **Sửa lỗi "Trả hàng nhà cung cấp" báo sai số lượng còn có thể trả + 2 lỗi liên quan phát hiện khi điều tra (2026-08-07, ngoài phase, theo phản hồi người dùng)**: người dùng báo cáo đã nhập 2 sản phẩm từ 1 NCC (tồn kho hiển thị đúng 2), dòng sản phẩm trên phiếu trả hiển thị đúng "Số lượng đã nhập" = 2, nhưng bấm Trừ kho luôn báo "vượt quá số đã nhập còn có thể trả" dù trả bao nhiêu cũng vậy. **Nguyên nhân gốc**: `createSupplierReturn({process:true})` (`backend/services/supplierReturn.service.js`) ghi `status='da_tru_kho'` **ngay lúc INSERT**, trước khi `applyProcessing()` chạy `validateRemaining()` — hàm này đếm "đã trả trước đó" bằng điều kiện `status='da_tru_kho'`, nên **tự đếm nhầm chính phiếu đang tạo** vào "đã trả", làm số còn lại bị trừ hụt đúng bằng số đang trả → luôn báo lỗi bất kể số lượng. Route hiển thị "Số lượng đã nhập" (`GET /supplier-returns/reference`) được gọi TRƯỚC khi phiếu tồn tại nên không bị ảnh hưởng — đây là lý do số hiển thị đúng nhưng validate lúc Trừ kho lại sai. Luồng 2 bước tách rời (Lưu trước, Trừ kho sau bằng `processSupplierReturn()`) không bị lỗi vì `applyProcessing()` chỉ đổi `status` thành `'da_tru_kho'` ở **cuối cùng** (sau khi validate xong) — bug chỉ lộ ra ở nút "Tạo + Trừ kho luôn" (1 bước). **Sửa**: luôn INSERT với `status='cho_tru_kho'`, để `applyProcessing()` tự đổi thành `'da_tru_kho'` sau khi validate — thống nhất hành vi với luồng 2 bước. **Đã kiểm tra và sửa luôn 2 vấn đề liên quan phát hiện trong lúc điều tra** (người dùng xác nhận qua `AskUserQuestion` trước khi làm): (1) **cùng lỗi y hệt** ở `stockReturn.service.js#createStockReturn()` ("Trả hàng xuất" khách hàng, đối xứng chiều ngược) — sửa cùng cách; (2) **lỗi khác phát hiện tình cờ**: `generateReceiptCode()`/`generateIssueCode()` (`stockReceipt.service.js`/`stockIssue.service.js`) lấy "mã gần nhất" bằng `ORDER BY id DESC LIMIT 1` **không lọc `is_return`** — vì `stock_receipts`/`stock_issues` dùng chung bảng với "Trả hàng xuất"/"Trả hàng NCC", nếu phiếu gần nhất theo id là 1 phiếu Trả hàng (mã `TH...`/`TN...`), `parseInt("TH000005".replace('PN',''))` trả về `NaN` → phiếu nhập/xuất **thường** tiếp theo bị sinh mã hỏng dạng `PN000NaN`/`PX000NaN`. Đã xác nhận **dữ liệu thật hiện chưa bị dính** (không có mã nào chứa "NaN" trong DB) nhưng có thể xảy ra bất kỳ lúc nào — sửa bằng cách thêm `WHERE is_return = 0` vào cả 2 hàm, đúng cách 2 hàm sinh mã "Trả hàng" (`generateReturnCode()`/`generateSupplierReturnCode()`) đã làm đúng từ đầu. Test qua service trực tiếp (dữ liệu test riêng, đã xóa sạch sau khi xong): tái hiện đúng kịch bản lỗi gốc (nhập 2, trả 2, Tạo+Trừ kho 1 bước) → trước sửa lỗi, sau sửa thành công; tái hiện đúng kịch bản mã NaN (tạo phiếu Trả hàng rồi tạo phiếu thường ngay sau) cho cả `PN`/`PX` → trước sửa ra "NaN", sau sửa ra mã hợp lệ. Không lỗi console/server, đã restart server sau khi sửa (bắt buộc, dự án không có hot-reload).

- ✅ **Giao diện di động — Đợt 1: Khung app (2026-08-06, code xong, chưa test điện thoại thật)**: dựng xong `frontend/m/` — Đăng nhập (`m/login.html`), Trang chủ (`m/index.html`: hero chào + 3 thẻ số liệu + sinh nhật trong tháng), 3 tab "sắp có" dùng chung 1 trang (`m/coming-soon.html?section=...`) cho Kho/Khách hàng/Dự án (chưa có nội dung nghiệp vụ, chờ Đợt 2/3). Khung điều hướng: app bar (`m-layout.js`) + thanh tab dưới cố định lọc theo `user.permissions` — **đọc lại `NAV_GROUPS` của `layout.js` desktop** qua hàm `_mTabModule()` thay vì hardcode danh sách quyền riêng, nên nếu sau này desktop đổi module_key của 1 nhóm menu thì tab mobile tự khớp theo, không cần sửa 2 nơi. Chuông thông báo (badge + bottom sheet liệt kê, tái dùng đúng API `/notifications` đã có). 10 thành phần UI của Đợt 0 hiện thực đầy đủ trong `frontend/m/assets/m-style.css`/`m-ui.js` (bottom sheet kéo-để-đóng, pull-to-refresh, skeleton...). **Tách `frontend/assets/tokens.css`** từ `style.css` — đây là **thay đổi DUY NHẤT chạm CSS desktop** (ngoài thêm hàm `isMobileDevice()` vào `api.js` và 2 dòng redirect trong `layout.js`/`auth.js`, thêm 6 icon mới vào `icons.js` không đụng icon cũ). **Phát hiện thiết bị đặt tại `api.js`** (khác dự tính ban đầu ghi ở `docs/TASK.md` là viết riêng 2 chỗ) — 1 định nghĩa `isMobileDevice()` dùng chung, gọi từ cả `layout.js`/`auth.js`, giảm trùng lặp. Test qua CDP (Chrome headless, giả lập `mobile:true` + touch — **phát hiện lỗi CDP**: bật đồng thời `setEmitTouchEventsForMouse` làm `Input.dispatchMouseEvent` treo vĩnh viễn, đã gỡ bỏ) xác nhận đúng: redirect thiết bị, đăng nhập, nội dung Trang chủ, bấm chuông/đóng sheet/chuyển tab bằng **click thật qua CDP** (không phải `.click()` JS), cờ "Dùng bản máy tính" hoạt động + được tôn trọng, **lọc quyền đúng với `thukho1`** (chỉ còn 3/5 tab), hồi quy desktop không đổi gì + không bị chuyển hướng nhầm — không lỗi console. **Chưa test được trên điện thoại thật qua LAN** (không có thiết bị vật lý trong môi trường hiện tại) — cần người dùng tự kiểm tra, đặc biệt vùng an toàn (safe-area) trên iPhone có notch/vạch home. **2 hạng mục phụ thuộc backend đã hỏi ngay** (khác dự tính "để riêng sau"): `rolling:true` + `maxAge` 8 tiếng→7 ngày đã làm luôn (theo lựa chọn người dùng, đã restart server + verify); đổi `MemoryStore` sang lưu đĩa người dùng chọn **để sau, gộp Phase 5 Go-live** — restart server hiện tại vẫn làm mất phiên đăng nhập của mọi thiết bị kể cả mobile.
- ✅ **Giao diện di động — Đợt 0: Thiết kế 10 thành phần giao diện mới (2026-08-06, không code, dùng skill `ui-ux-pro-max`)**: thanh tab dưới, app bar + nút quay lại, thẻ danh sách (thay `.data-table`), bottom sheet kéo-để-đóng (thay `.modal-overlay`), ô tìm kiếm dính đầu trang, segmented control (thay tab ngang `project-detail.html`), pull-to-refresh, skeleton loading, hàng thông tin kiểu Settings (màn chi tiết), nút hành động chính (FAB) — mỗi thành phần trình bày riêng, người dùng duyệt lần lượt trước khi sang cái tiếp theo. Toàn bộ **dùng lại token màu/font/radius/shadow hiện có** (không phát minh bộ nhận diện mới — lần đầu chạy `--design-system` của skill trả về gợi ý sai hẳn hướng "app store landing page" + font academia, đã bỏ qua và tự tổng hợp từ checklist UX chuẩn của skill + token thật đọc từ `style.css`). Đã ghi đầy đủ vào `docs/DESIGN-SYSTEM.md` mục "Giao diện di động". **Đợt 0 hoàn thành — sẵn sàng cho Đợt 1 (khung app: tách `tokens.css`, `m-layout.js`/`m-ui.js`, phát hiện thiết bị, đăng nhập + Trang chủ mobile).**
- ✅ **Mẫu in Phiếu xuất kho: dạng hiển thị ngân hàng + sửa lỗi CSS chết khiến "Tổng cộng" không căn phải (2026-08-06, ngoài phase, theo phản hồi người dùng kèm ảnh)**: dòng ngân hàng bỏ ngoặc đơn quanh chi nhánh, nay **dùng chung `buildBankNameBranchLine()`** với mẫu "Giấy đề nghị tạm ứng" → `Vietcombank – PGD An Nhơn` (một nguồn duy nhất, thêm mẫu in thứ 3 sau này không phải sửa lại). **Phát hiện quan trọng hơn khi điều tra**: 2 rule `.print-total-label`/`.print-total-value` (độ ưu tiên `0,1,0`) **bị `.print-table td` (`0,1,1`) ghi đè từ lâu** → dòng "Tổng cộng" thực tế luôn **căn trái** và **cỡ chữ 15px chưa bao giờ có hiệu lực** (luôn 13px), trong khi các ô số cùng cột lại đúng nhờ `.print-table td.print-num` (`0,2,1`) đủ mạnh. Nhìn ảnh chụp **không thấy được** (số tiền mẫu gần lấp đầy ô, lệch chỉ ~2px) — chỉ lộ ra khi đo computed style + `Range.getBoundingClientRect()` thật qua CDP. Đã sửa bằng cách viết đúng dạng `.print-table td.<class>`. **Bài học ghi lại**: mọi rule CSS nhắm vào `<td>`/`<th>` trong `.print-table` phải viết kèm tiền tố `.print-table td.`, không dùng class trần. Test đo hình học thật qua CDP trên cả khung xem trước, **trang in thật PX000016** (mép phải khớp tuyệt đối 0px với các số cùng cột) và hồi quy "Giấy đề nghị tạm ứng" (không đổi) — không lỗi console, chỉ đọc, không ghi đè tùy chỉnh mẫu in thật của người dùng.
- 📋 **Giao diện di động — ĐÃ CHỐT KẾ HOẠCH, CHƯA VIẾT DÒNG CODE NÀO (2026-08-06, ngoài phase, theo yêu cầu người dùng)**: người dùng yêu cầu giao diện cho điện thoại/tablet, nói rõ **không phải web thu nhỏ** mà là "1 giao diện hoàn toàn dùng để chạy trên các thiết bị di động và tablet, bố cục và hiển thị giống như các app native". Đã khảo sát hiện trạng (34 trang HTML, 44 file JS ~10.900 dòng, `style.css` 3.679 dòng, **đúng 1 media query cho mobile** — thực tế chưa có gì cho di động), phân tích 3 phương án kiến trúc, chốt **4 quyết định qua `AskUserQuestion`** rồi ghi kế hoạch vào tài liệu. **Kiến trúc đã chọn**: bản app **RIÊNG tại `frontend/m/`**, dùng chung API + cookie session, **không sửa frontend desktop đang chạy production** (2 phương án khác — chỉ thêm media query, và một-bộ-trang-hai-chế-độ-render — đã cân nhắc và loại bỏ; xem lý do đầy đủ ở `docs/DECISIONS.md`). Giữ mô hình **MPA** (mỗi màn 1 file HTML, không tự viết SPA router). **Không cần sửa `backend/server.js`** — `express.static(frontend/)` đã phục vụ sẵn `frontend/m/` tại `/m/`; **không thêm API nào**. **4 quyết định người dùng đã chốt**: (1) phạm vi Tra cứu + Dự án làm trước, nghiệp vụ ghi (lập phiếu kho/thu chi/thanh toán) để ngỏ đánh giá lại sau; (2) **không bật HTTPS** — hệ quả đã nêu rõ và được chấp nhận: iOS thêm vào màn hình chính chạy toàn màn hình gần như native, **Android vẫn còn thanh địa chỉ Chrome**, cả 2 không có offline/push (bật HTTPS sau này không phải làm lại giao diện); (3) điện thoại trước, tablet dùng chung khung + lưới 2 cột; (4) chỉ trong LAN, không truy cập 4G. **5 đợt**: Đợt 0 thiết kế 10 thành phần UI mới qua `ui-ux-pro-max` → Đợt 1 khung app (nặng nhất: tách `tokens.css` dùng chung, `m-layout.js`/`m-ui.js`, phát hiện thiết bị + chuyển hướng, đăng nhập + Trang chủ) → Đợt 2 Tra cứu 7 màn chỉ đọc → Đợt 3 Dự án tại công trường → Đợt 4 nghiệp vụ ghi (để ngỏ). Chi tiết: `docs/PRD.md` 4.16, `docs/Plan.md` mục 4, `docs/TASK.md`, `docs/DECISIONS.md`.
- ✅ **3 chỉnh sửa mẫu in: đậm-nghiêng "Số tiền bằng chữ", bỏ chữ "Chi nhánh" thừa, popup xem trước khi in (2026-08-06, ngoài phase, theo phản hồi người dùng)**: token `advance_amount_words` (mới đăng ký ở mục ngay dưới) nay đã **đặt sẵn** vào mẫu "Giấy đề nghị tạm ứng" (dòng mới dưới "Số tiền", bọc `<strong><em>`) — đã đồng bộ cả bản mặc định (`printTemplateTokens.js`) lẫn dòng dữ liệu thật đang lưu (không mất tùy chỉnh gì vì trước đó chưa bị người dùng sửa). `buildBankNameBranchLine()` bỏ chữ "Chi nhánh " thừa trước tên chi nhánh. **Thay đổi lớn nhất**: nút "In phiếu"/"In giấy đề nghị tạm ứng" trước đây mở tab trình duyệt mới (`<a target="_blank">`) — nay mở **popup toàn màn hình ngay trên trang đang đứng** (`frontend/assets/print-preview.js` mới, dùng chung) chứa 1 `<iframe>` nhúng `print-issue.html`/`print-project-advance.html` (2 trang này giữ nguyên 100% code, không nhân bản logic render). Nút "Quay lại" bên trong popup tự đóng popup (không điều hướng); popup **tự động ẩn** ngay sau khi hộp thoại in đóng lại (lắng nghe `afterprint` trên iframe cùng gốc). Test qua trình duyệt thật (Chrome headless, click thật, dữ liệu có sẵn PX000016 + đợt thanh toán DA000003): xác nhận 0 tab mới được mở, popup/Quay lại/tự-ẩn-sau-khi-in đều đúng, 2 dòng nội dung sửa hiển thị đúng, không lỗi console. **Lỗi phát hiện ngay sau đó qua phản hồi người dùng kèm ảnh chụp**: bản đầu mắc đúng lỗi CSS `[hidden]` bị `display` đè (lỗi đã lặp lại nhiều lần trong dự án) trên `.print-preview-modal` — popup luôn hiển thị phủ kín màn hình bất kể trạng thái, "Quay lại" chỉ dọn trắng iframe chứ không ẩn được khung ngoài. Đã sửa bằng `:not([hidden])`, test lại lần này bằng computed style + bounding box thật qua CDP (không chỉ đọc `.hidden` trong JS như lượt đầu — chính cách test thiếu sót đó để lọt lỗi).
- ✅ **Cấu hình mẫu in: token "Số tiền tạm ứng bằng chữ" + đính kèm hình ảnh tự do (2026-08-06, ngoài phase, theo yêu cầu người dùng)**: mẫu "Giấy đề nghị tạm ứng" có thêm token `advance_amount_words` (số tiền đọc thành chữ, dùng lại `numberToVietnameseWords()` có sẵn) chèn được tự do qua dropdown "Chèn trường thông tin" — không tự chèn sẵn vào mẫu mặc định. Trang "Chỉnh sửa mẫu in" có thêm nút "Chèn hình ảnh" (dùng chung cho mọi loại mẫu in) cho phép tải 1 file ảnh (PNG/JPG/WEBP/GIF, ≤3MB) lên và tự chèn `<img>` đúng vị trí con trỏ trong vùng soạn thảo — ví dụ dùng để dán mã QR chuyển khoản vào mẫu "Giấy đề nghị tạm ứng". File ảnh lưu thật trên đĩa tại `data/print-template-uploads/<loại mẫu>/` (thư mục mới, không version-control — xem `.gitignore`), phục vụ qua route tĩnh công khai `/uploads/print-templates/...`. Không cần migration (ảnh là nội dung HTML thô trong `header_html`/`footer_html` có sẵn, không phải token/cột mới). Test qua API (curl: upload hợp lệ/sai định dạng/thiếu file/quyền) và trình duyệt thật (Chrome headless, **click thật** qua CDP `Input.dispatchMouseEvent` + bắt `Page.fileChooserOpened` để cấp file — đúng luồng người dùng thật, không bypass code mới viết): chèn đúng vị trí, hiện đúng trong khung xem trước, lưu/đọc lại đúng, không lỗi console. Mẫu in đã khôi phục mặc định + xóa file test trên đĩa sau khi xong.
- ✅ **Thêm nhanh khách hàng ngay trên form "Thêm dự án mới" (2026-08-06, ngoài phase, theo yêu cầu người dùng)**: select "Khách hàng" (`projects.html`) thêm lựa chọn `+ Thêm khách hàng mới` — chọn sẽ hiện khối `.new-partner-inputs` (Tên/SĐT/Địa chỉ) ngay dưới, dùng đúng pattern "thêm nhanh đối tác" đã có sẵn ở `stock-issues.html`/`stock-receipts.html` (tái dùng nguyên class CSS, không thêm CSS mới). Lúc lưu, nếu đang chọn `__new__`: gọi `POST /partners` (`type='khach_hang'`) trước để lấy `id` thật, rồi mới gửi `POST`/`PUT /projects` với `partner_id` đó — sau khi lưu thành công tải lại cả danh sách dự án lẫn danh sách khách hàng (để lần mở modal tiếp theo select đã có tên mới). **Phát hiện + sửa 1 lỗi phân quyền phát sinh khi làm tính năng này**: `POST /api/partners` trước đây chỉ cho phép quyền `kho` hoặc `cong_no` — tài khoản chỉ có quyền `du_an` (không seed kèm 2 quyền kia theo thiết kế module Dự án) sẽ bị chặn 403 khi bấm nút này; đã hỏi và người dùng chọn mở rộng `requireAnyPermission(['kho','cong_no','du_an'])` cho route đó. Test qua trình duyệt thật (Chrome headless CDP thô): mở modal → chọn "+ Thêm khách hàng mới" → khối nhập hiện đúng → điền tên/SĐT/địa chỉ → lưu → xác nhận qua API khách hàng mới được tạo đúng SĐT/địa chỉ VÀ dự án được gắn đúng `partner_id` đó, không lỗi console. Dữ liệu test (1 dự án + 1 khách hàng) đã xóa sạch ngay sau test.
- ✅ **Hạ tầng ghi log chẩn đoán: log server ra file + bắt request chậm + bắt lỗi JS trình duyệt (2026-08-06, ngoài phase, theo yêu cầu người dùng ngay sau mục điều tra "đơ ứng dụng" bên dưới)**: người dùng hỏi "nếu đang chạy trên máy khách hàng có cách nào xem log không" — phát hiện trước đó ứng dụng hoàn toàn không ghi log ra file (chỉ `console.log`/`console.error`, mất hết khi đóng cửa sổ terminal/portable `start.bat`, chưa có PM2 trên máy chủ thật). Đã hỏi và người dùng chọn làm cả 3: (1) `backend/utils/logger.js` (mới) — ghi log ra `data/logs/server-YYYY-MM-DD.log` (xoay theo ngày, tự dọn >14 ngày giống `scripts/backup.js`) song song với console; (2) `server.js` — bắt `uncaughtException`/`unhandledRejection` (ghi log rồi thoát, đúng hành vi mặc định cũ của Node khi không có handler, chỉ khác là nay có ghi lại lý do trước khi thoát) + middleware ghi log request nào ≥2 giây; (3) `backend/routes/clientLogs.routes.js` (mới, `POST /api/client-logs`, KHÔNG đòi đăng nhập vì lỗi có thể xảy ra trước khi đăng nhập) nhận lỗi JS từ trình duyệt. **Quyết định vị trí quan trọng**: gắn `window.addEventListener('error'/'unhandledrejection', ...)` vào `frontend/assets/api.js` (không phải `layout.js`) vì `api.js` là file DUY NHẤT được nạp trên MỌI trang kể cả `login.html`/`setup.html` (layout.js chỉ nạp sau khi đăng nhập, không có sidebar) — có dedupe theo `message+url` trong phạm vi 1 lần tải trang, tránh gửi lặp nếu 1 lỗi lặp lại liên tục. `.gitignore` thêm `data/logs/`. Test qua API (curl: POST giả lập lỗi JS ghi đúng vào file) + trình duyệt thật (Chrome headless CDP thô, KHÔNG đăng nhập, ném lỗi JS thật + Promise reject thật ngay trên `login.html`) — cả 2 đều ghi đúng vào log kèm URL/user-agent/stack, kể cả khi chưa đăng nhập; restart server xác nhận không hồi quy (login/API bình thường vẫn hoạt động đúng). Middleware request chậm chưa có cách an toàn để giả lập 1 request thật ≥2s trên dữ liệu hiện có nên chỉ xác nhận qua đọc code (pattern `res.on('finish')` chuẩn, rủi ro thấp), chưa test trực tiếp.
- ✅ **Điều tra + sửa lỗi "đơ ứng dụng" báo cáo từ 1 người dùng, đặc biệt ở tab "Thanh toán & Công nợ" trong Dự án (2026-08-06, ngoài phase, theo phản hồi người dùng)**: đã điều tra kỹ (đọc code, cấu hình SQLite/session, stress test thật qua trình duyệt headless 300 lần mở/đóng modal "Thêm đợt thanh toán" + chuyển tab liên tục — không tái hiện được, 0 lỗi console, JS heap không tăng, dữ liệu thật hiện quá nhỏ để gây chậm do khối lượng). Người dùng xác nhận **chỉ 1 người bị đơ cùng lúc** (người khác vẫn thao tác bình thường) — loại trừ khả năng nghẽn server toàn cục (SQLite chạy đồng bộ 1 luồng, nếu nghẽn sẽ ảnh hưởng MỌI người cùng lúc). **Phát hiện 1 lỗi thật khi đọc kỹ `renderGanttChart()`** (`frontend/assets/project-detail.js`, chạy lại mỗi lần `loadProjectDetail()` — tức mỗi lần tải trang VÀ sau MỌI thao tác thêm/sửa/xóa ở bất kỳ tab nào trong trang dự án, không riêng tab "Giai đoạn"): vòng lặp vẽ nhãn tháng trên biểu đồ Gantt trước đây KHÔNG có giới hạn số lần lặp — nếu 1 giai đoạn bị nhập nhầm năm ở "Ngày bắt đầu/hoàn thành dự kiến" (vd gõ `9999` hoặc `2260` thay vì `2026`), khoảng cách ngày bị thổi phồng lên hàng trăm/nghìn năm, vòng lặp chạy hàng chục nghìn lần dựng chuỗi SVG khổng lồ → đơ cứng tab trình duyệt, đúng khớp triệu chứng người dùng mô tả. Đã kiểm tra dữ liệu thật hiện có (18 giai đoạn) — chưa có giai đoạn nào bị lỗi ngày như vậy ngay lúc kiểm tra, nên đây có thể chưa phải nguyên nhân **đang** xảy ra nhưng là lỗi tiềm ẩn có thật, người dùng xác nhận sửa phòng ngừa ngay. **Sửa**: thêm chặn an toàn `MAX_MONTH_TICKS = 1000` (~83 năm, dư dả cho bất kỳ dự án thật nào) vào vòng lặp vẽ nhãn tháng — không ảnh hưởng hiển thị bình thường. Test qua trình duyệt thật (Chrome headless CDP thô, dữ liệu test riêng không đụng dự án thật): mô phỏng đúng lỗi (1 giai đoạn 2026 + 1 giai đoạn 9999, chênh ~7973 năm) — trước đây sẽ lặp ~95.676 lần, sau khi sửa trang tải trong ~1.5s và nút "Thêm đợt thanh toán" vẫn phản hồi đúng ngay sau đó; test lại 300 lần mở/đóng modal bình thường vẫn đúng 300/300, không hồi quy. **Chưa xác nhận được đây có phải nguyên nhân gốc của lần báo cáo cụ thể hay không** (không có thêm thông tin chẩn đoán như tên dự án/thời gian mở tab/ảnh chụp console lúc xảy ra) — nếu còn tái diễn, cần thêm thông tin đó hoặc theo dõi máy/trình duyệt của người dùng bị ảnh hưởng để tìm nguyên nhân khác (máy yếu, mạng LAN kém, v.v.).
- ✅ **Sửa lỗi hiển thị: khung chọn giá nhập hiện thừa dù trống ở "Trả hàng nhà cung cấp" (2026-08-06, ngoài phase, theo phản hồi người dùng)**: `.price-picker-hint` luôn hiện 1 khung viền xanh rỗng ngay dưới ô "Giá nhập" kể cả khi chưa chọn NCC/sản phẩm — đúng dạng lỗi CSS `[hidden]` bị `display` đè đã gặp nhiều lần trong dự án (`.alert`/`.form-row`/`.notification-panel`/`.page-header-actions`), lần này bỏ sót khi viết mới class này. Sửa bằng cách tách `display:flex` sang rule riêng `.price-picker-hint:not([hidden])`. Test qua trình duyệt thật (Chrome headless CDP thô, computed style + geometry, không dùng dữ liệu thật): trạng thái mặc định `display:none`/0×0 đúng; trạng thái có ≥2 giá lịch sử (NCC "Cong ty CP Thep Hoa Phat" + sản phẩm SP020) vẫn hiện đúng khung + đủ 2 chip giá — không hồi quy tính năng.
- ✅ **"Trả hàng nhà cung cấp" — mở rộng trang "Trả hàng" thành quản lý chung 2 loại phiếu (2026-08-06, ngoài phase, theo yêu cầu người dùng)**: thêm loại phiếu thứ 2 **đối xứng chiều ngược lại** "Trả hàng xuất" — trả hàng VỀ nhà cung cấp làm **giảm** tồn kho (như phiếu xuất) + **giảm** công nợ phải trả NCC. Tái dùng `stock_issues`/`stock_issue_items` (đánh dấu `is_return=1`, migration `034`, cùng lý do CHECK constraint đã áp dụng cho `stock_receipts` ở "Trả hàng xuất") thay vì tạo bảng mới; mã riêng prefix `TN...`; `unit_price` của `stock_issue_items` dùng đúng nghĩa sẵn có để lưu "Giá nhập" (không cần cột mới, khác `stock_receipt_items` trước đó cần thêm `sale_price` vì `unit_price` ở đó bị chiếm dụng cho giá vốn tự tính). `debt.service.js#recordReturnCredit()` tổng quát hóa nhận thêm `referenceType` để dùng chung cho cả 2 chiều (`'receipt'`/`'issue'`). Giá vốn xuất kho tính qua đúng `consumeStockForIssue()` (FIFO/bình quân gia quyền) như phiếu xuất thường — khác "Trả hàng xuất" (dùng giá vốn bình quân snapshot), vì đây thực chất là 1 nghiệp vụ xuất kho thật. **Tính năng mới trong form**: ô "Giá nhập" tự tra cứu lịch sử giá đã mua từ đúng NCC cho đúng sản phẩm — 1 giá thì tự điền, ≥2 giá thì hiện chip chọn (pattern UI mới, đã tư vấn qua skill `ui-ux-pro-max`). Trang "Trả hàng" nay **gộp chung 1 danh sách** 2 loại phiếu (cột "Loại", gộp ở tầng frontend từ 2 API `/stock-returns` + `/supplier-returns`), nút "+ Lập phiếu trả hàng" đổi thành dropdown 2 lựa chọn (pattern UI mới thứ 2, cũng tư vấn qua `ui-ux-pro-max`) mở đúng 1 trong 2 form. Cùng quy trình 2 bước Lưu/Trừ kho như "Trả hàng xuất". **Lỗi phát hiện + đã sửa khi test**: `GET /stock-issues` (danh sách Phiếu xuất kho) ban đầu quên lọc `is_return=0`, để lộ phiếu trả NCC lẫn vào — đã thêm filter giống `stockReceipts.routes.js` đã làm trước đó. Test qua API (Node `fetch`) + trình duyệt thật (Chrome headless CDP thô): tra giá 1/nhiều giá đúng, chip chọn giá điền đúng, "Đã nhập" tính đúng, Lưu/Sửa/Trừ kho đúng luồng, tồn kho giảm đúng + công nợ NCC giảm đúng sau khi trừ kho, không lẫn trong Phiếu xuất kho, badge "Trả hàng" hiện đúng ở trang Công nợ NCC, danh sách gộp hiển thị đúng cả 2 loại, không lỗi console.
- ✅ **"Trả hàng": bổ sung quy trình 2 bước "Lưu" (nháp) / "Trừ kho" (thực thi) (2026-08-06, ngoài phase, theo yêu cầu người dùng ngay sau khi hoàn thành module "Trả hàng xuất")**: đổi tên menu + tiêu đề trang từ "Trả hàng xuất" thành **"Trả hàng"**. Migration `033` thêm `stock_receipts.status` (`cho_tru_kho`/`da_tru_kho`, mặc định `da_tru_kho` — phiếu nhập kho thường không bị ảnh hưởng). Modal lập phiếu có 2 nút: **"Lưu"** (`POST`/`PUT` không `process`, chỉ ghi thông tin, không đụng `stock_movements`/`stock_lots`/`debt_ledger`) và **"Trừ kho"** (thực thi đúng nguyên vẹn logic nghiệp vụ cũ — validate số lượng còn lại, tính giá vốn bình quân, ghi giảm công nợ — qua hàm dùng chung `applyProcessing()` trong `stockReturn.service.js`, gọi từ `POST /stock-returns` với `process:true` khi tạo mới, hoặc `POST /stock-returns/:id/process` khi xử lý phiếu đã lưu trước đó). Phiếu `cho_tru_kho` hiện icon "Sửa" (mở modal sửa, gọi `PUT /stock-returns/:id`) + icon "Trừ kho" (duyệt nhanh ngay trên danh sách, có `confirm()`) ngay trên danh sách; sau khi trừ kho thành công chỉ còn icon "Xem chi tiết" — khóa vĩnh viễn. `getReturnReference()` (tính "Số lượng đã xuất còn có thể trả") chỉ tính các phiếu đã `da_tru_kho` vào "đã trả", tránh 1 phiếu đang chờ duyệt chặn nhầm phiếu khác. Test qua API (Node `fetch`) đầy đủ: Lưu không đụng tồn kho/công nợ, sửa cập nhật đúng, Trừ kho dùng đúng số liệu MỚI NHẤT sau khi sửa (không phải số liệu lúc tạo), khóa đúng sau khi trừ kho (chặn sửa/trừ kho lại đều 400), "Lưu" không chặn vượt số còn lại nhưng "Trừ kho" vẫn chặn đúng. Test qua trình duyệt thật (Chrome headless CDP thô): tiêu đề trang/menu đổi đúng "Trả hàng", tạo phiếu qua UI → Lưu → Sửa số lượng → Trừ kho qua icon danh sách → khóa đúng (icon Sửa/Trừ kho biến mất), số liệu trừ kho đúng theo giá trị đã sửa, không lỗi console.
- ✅ **"Trả hàng xuất" — khách hàng trả lại hàng đã mua (2026-08-06, ngoài phase, theo yêu cầu người dùng, đã lên kế hoạch qua `EnterPlanMode` + chốt 4 quyết định phạm vi qua `AskUserQuestion` trước khi code)**: trang mới `stock-returns.html` (menu Kho, sau "Xuất kho") cho lập "Phiếu trả hàng xuất" — chọn Khách hàng + Công trình (tự lọc theo đúng khách hàng đang chọn, không phải toàn bộ dự án hệ thống) + từng dòng sản phẩm (Mã SP/Tên SP/ĐVT tự điền, "Số lượng đã xuất" hệ thống tự tính chỉ đọc — chặn cứng nếu "Số lượng trả lại" vượt quá, "Giá bán" tự điền theo `product.sale_price` sửa tay được). **Quyết định kiến trúc quan trọng nhất**: tái dùng đúng cơ chế `stock_receipts`/`stock_receipt_items`/`stock_movements`/`stock_lots` đã có (đánh dấu cờ mới `is_return=1`, mã riêng prefix `TH...`) thay vì tạo bảng mới — vì `stock_movements.reference_type` (sổ cái tồn kho) và `debt_ledger.reference_type` đều có `CHECK` chỉ nhận `('receipt','issue'[,'payment'])`, thêm giá trị `'return'` sẽ đòi dựng lại 2 bảng quan trọng nhất hệ thống (rủi ro không chấp nhận được) — xem đầy đủ `docs/DECISIONS.md`. Công nợ khách hàng **giảm** qua hàm mới `debt.service.js#recordReturnCredit()` (`type='tra', reference_type='receipt'`, KHÔNG dùng `recordDebtFromDocument()` vì hàm đó luôn ghi tăng nợ); giá vốn lô hàng trả về kho tự động lấy giá vốn bình quân gia quyền hiện tại (`costing.service.js#getWeightedAverageCost()`), không dùng giá bán. Phiếu trả **tách biệt hoàn toàn** khỏi danh sách "Phiếu nhập kho" (`GET /stock-receipts` lọc `is_return=0`) nhưng **tự động** vẫn hiện đúng trong tab "Vật tư" của dự án (nhãn riêng "Trả hàng") nhờ vẫn là 1 dòng `stock_receipts` có `project_id`, không cần sửa cơ chế UNION đã có. Lịch sử giao dịch ở trang "Công nợ khách hàng" hiện badge riêng "Trả hàng" (nhận diện qua tổ hợp `type='tra' + reference_type='receipt' + is_adjustment=0`, tổ hợp trước đây không thể xảy ra nên không cần thêm cột mới). Migration `032` (`stock_receipts.is_return`, `stock_receipt_items.sale_price`). Test qua API (Node `fetch`): tồn kho tăng đúng "Số lượng trả lại", công nợ giảm đúng, chặn đúng khi trả vượt số đã xuất còn lại, chặn đúng đối tác không phải khách hàng, không xuất hiện ở `GET /stock-receipts`. Test qua trình duyệt thật (Chrome headless CDP thô): lập phiếu đầy đủ luồng (khách hàng → công trình tự lọc → sản phẩm → số lượng đã xuất hiện đúng → lưu), tìm kiếm đúng theo mã phiếu/tên/SĐT, badge "Trả hàng" hiện đúng ở Công nợ khách hàng, nhãn "Trả hàng" hiện đúng ở tab Vật tư dự án, không lỗi console.
- ✅ **Thêm nhóm trường "Thông tin phiếu in" cho Đợt thanh toán dự án (2026-08-06, ngoài phase, theo yêu cầu người dùng)**: modal "Thêm/Sửa đợt thanh toán" (`project-detail.html`, tab "Thanh toán & Công nợ") có thêm nhóm `.section-heading` "Thông tin phiếu in" gồm 2 trường không bắt buộc — "Gửi đến" (nhập tự do tên người nhận phiếu) và "Danh xưng" (combobox Anh/Chị/Công Ty/Đơn vị). Migration `031` thêm `project_payment_milestones.recipient_name`/`recipient_title` (mã tiếng Việt không dấu, `CHECK` giới hạn giá trị). 2 trường này đăng ký làm token mới (`recipient_name`/`recipient_title`) trong mẫu in "Giấy đề nghị tạm ứng" (`backend/config/printTemplateTokens.js`) để người dùng tự chèn vào vị trí mong muốn qua trang "Cấu hình mẫu in" (không hardcode sẵn vào mẫu mặc định, đúng yêu cầu "để tôi có thể thêm vào mẫu in phiếu"). Test qua API (Node `fetch`) + trình duyệt thật (Chrome headless CDP thô): modal hiện đúng nhóm trường, dropdown "Chèn trường thông tin" có đủ 2 token mới, chèn thử vào đầu trang cho ra đúng giá trị mẫu ở khung xem trước — không lưu đè mẫu in thật đã bị người dùng tự tùy chỉnh trước đó.
- ✅ **In "Giấy đề nghị tạm ứng" theo đợt thanh toán dự án (2026-08-05, ngoài phase, theo yêu cầu người dùng kèm mẫu PDF thật, đã lên kế hoạch qua `EnterPlanMode` + chốt 1 quyết định qua `AskUserQuestion` trước khi code)**: mẫu in **thứ 2** trong hệ thống "Cấu hình mẫu in" (sau "Phiếu xuất kho") — icon "In giấy đề nghị tạm ứng" mới trên từng dòng "Đợt thanh toán" ở trang chi tiết dự án, mở `print-project-advance.html` (trang mới). **Nhân dịp này tổng quát hóa `print-template-edit.js`** để dùng chung được cho loại phiếu KHÔNG có bảng sản phẩm (cờ `hasTable` mới, tự ẩn khối "Cột bảng sản phẩm"/checkbox "Số tiền viết bằng chữ" khi không cần) — không cần `ALTER TABLE`, bảng `print_templates` đã đủ tổng quát, migration `030` chỉ seed 1 dòng mới. Token lấy từ `projects.contract_no`/`contract_date`/`site_address`, `partners.name`, `project_payment_milestones.sort_order`/`percent`/`amount`, `company_settings` (ngân hàng) — ngày in luôn là **ngày hiện tại lúc bấm in**, không phải ngày tạo đợt thanh toán. Phần "Đại diện bởi"/tên người ký là **văn bản tĩnh gõ thẳng trong mẫu** (người dùng chọn không thêm trường mới vào Thông tin công ty vì "cố định"). **Đã áp dụng ngay các bài học từ lần làm mẫu in trước** (không để chip token lọt ra bản in, chữ chính màu đen không nhạt, in đậm đúng các trường động, dùng đúng class nút đã sửa) — xem đầy đủ `docs/DECISIONS.md`. Test qua API + trình duyệt thật (dữ liệu thật dự án `DA000001`) + **hồi quy bắt buộc mẫu `stock_issue`** (chỉ test đọc, không đụng tới mẫu thật đã bị người dùng tự chỉnh trước đó) — không lỗi console, không hồi quy.
- ✅ **Sửa 3 lỗi modal "Ghi nhận thanh toán" — luồng "Ghi nhận đã thu" từ Dự án (2026-08-05, ngoài phase, theo phản hồi người dùng kèm ảnh chụp)**: (1) 2 ô "Dự án"/"Đợt thanh toán" bị lệch hàng do nhãn dài ngắn khác nhau (1 dòng vs 2 dòng) — sửa `.form-row > .form-field > label` dùng `min-height:38px` + neo đáy, áp dụng chung mọi `.form-row` trong hệ thống; (2) ô "Số tiền" tự điền theo đúng số còn phải thu của đợt thanh toán đã chọn (`customer-debts.js` cache lại danh sách đợt vừa tải để tra cứu, áp dụng cả khi tự chọn tay lẫn khi mở sẵn từ URL preset); (3) khóa hẳn "Ghi nhận đã thu" khi đợt đã "Đã thu đủ" — khóa ở cả nguồn (`project-detail.js`, nút trên từng dòng đợt tự disable) lẫn đích (`customer-debts.js`, khóa ô Số tiền/Ghi chú/nút Ghi nhận + thông báo rõ lý do nếu truy cập URL preset trực tiếp tới 1 đợt đã thu đủ). Test qua trình duyệt thật (CDP thô, chỉ đọc — không tạo bản ghi công nợ thật) với dữ liệu thật sẵn có (dự án `DA000003`), không lỗi console.
- ✅ **Cấu hình mẫu in — bắt đầu với Phiếu xuất kho (2026-08-05, ngoài phase, theo yêu cầu người dùng, đã lên kế hoạch qua `EnterPlanMode` + chốt 3 quyết định phạm vi qua `AskUserQuestion` trước khi code)**: trang mới `print-templates.html` (menu Cấu hình) liệt kê các **loại phiếu** đã có mẫu trong hệ thống (hiện chỉ "Phiếu xuất kho", không có nút tạo mẫu tùy ý — mỗi loại phiếu cần có sẵn 1 trang render tương ứng trong code) + trang `print-template-edit.html` (trình soạn thảo WYSIWYG 2 cột: trái là toolbar định dạng chữ + 2 vùng `contenteditable` "Đầu trang"/"Chân trang" cho phép chèn "chip" trường thông tin (token) vào bất kỳ vị trí nào, phải là khung xem trước trực quan cập nhật tức thời bằng dữ liệu mẫu cố định). Migration `028` (bảng `print_templates`, 1 dòng/loại phiếu, seed sẵn dựng lại **đúng y hệt** layout tĩnh cũ — không đổi gì hiển thị cho tới khi người dùng chủ động sửa). **3 quyết định phạm vi đã chốt** (khác ảnh KiotViet người dùng gửi tham khảo): bảng sản phẩm chỉ bật/tắt + sắp xếp cột có sẵn (không tự vẽ bảng tay, giảm rủi ro hỏng cấu trúc khi in thật); chỉ khổ A4 dọc/ngang (không làm khổ giấy in nhiệt K58/K80); mỗi loại phiếu đúng 1 mẫu sửa trực tiếp (không có khái niệm nhiều mẫu/loại + chọn "đang dùng") — xem đầy đủ `docs/DECISIONS.md`. `frontend/print-issue.html`/`.js` viết lại hoàn toàn phần render (bỏ markup tĩnh hardcode, nay dựng động từ mẫu đã lưu qua `frontend/assets/print-template-render.js` — file dùng CHUNG giữa trang in thật và khung xem trước của trang chỉnh sửa, tránh 2 nơi lệch logic). Test qua API (curl) + trình duyệt thật (Chrome headless CDP thô): trang danh sách/trình soạn thảo hoạt động đúng (chèn/xóa token, tắt/sắp xếp cột, đổi khổ giấy, lưu, đặt lại), **và quan trọng nhất — phiếu xuất kho thật có sẵn (PX000016) render khớp đúng 100% với dữ liệu gốc** sau khi đổi cơ chế, không lệch trường/thứ tự cột, không lỗi console. Mẫu in đã khôi phục về đúng bản seed mặc định sau khi test xong. **Bổ sung ngay sau đó (cùng ngày, theo phản hồi người dùng)**: thêm chỉnh **độ rộng từng cột** bảng sản phẩm (migration `029`, `table_columns` đổi sang mảng object `{key,width}` — tỉ lệ tương đối tự quy về 100% lúc render, dữ liệu cũ dạng mảng chuỗi vẫn đọc được bình thường; `.print-table` đổi `table-layout:fixed` để cột giữ đúng độ rộng, chữ dài tự xuống dòng trong ô) + thêm dòng **"Số tiền viết bằng chữ"** dưới dòng Tổng cộng (`numberToVietnameseWords()` mới, thuần JS đọc số tiếng Việt đúng quy tắc — cố định trong `tfoot` bảng, chỉ bật/tắt qua 1 checkbox riêng, không phải token rời dễ bị xóa nhầm). **Phát hiện khi test đợt này**: dữ liệu mẫu in thật đã bị người dùng tự chỉnh qua UI thật giữa lúc đang phát triển tiếp (không phải lỗi) — đã dừng ngay các test kiểu ghi/reset, chỉ còn xác nhận qua đọc dữ liệu để không ghi đè tùy chỉnh thật của người dùng. **Sửa tiếp 3 lỗi hiển thị người dùng phát hiện ngay sau đó (cùng ngày)**: (1) lỗi nghiêm trọng nhất — chip token màu xanh (thiết kế chỉ dành cho lúc soạn thảo) bị "rò rỉ" ra cả bản in thật lẫn khung xem trước do `applyPrintTemplateTokens()` chỉ đổi chữ mà không bỏ class — sửa bằng cách thay hẳn `<span>` bằng text node thuần, chip màu giờ chỉ còn thấy trong vùng soạn thảo; (2) tiêu đề cột bảng in đổi sang căn giữa; (3) nút "Quay lại" (thẻ `<a>` đầu tiên trong `.page-header-actions`) bị nén/xuống dòng — bổ sung `white-space:nowrap`/`flex-shrink:0`/`text-decoration:none` dùng chung cho mọi trang. **Sửa thêm 3 ý ngay sau đó**: nút "Lưu mẫu" quá khổ (do `.btn-primary` mặc định `width:100%`, thêm `width:auto` khi đặt trong `.page-header-actions`); thông tin công ty trên bản in quá nhạt (đổi `.print-header p` từ xám nhạt sang đen + `font-weight:500`); bỏ chữ "đ" thừa sau số tổng cộng trong bảng (giữ nguyên "đồng" ở dòng số tiền viết bằng chữ).
- ✅ **Hệ thống thông báo (2026-08-05, ngoài phase, theo yêu cầu người dùng — hoàn thành phiên bản đầu)**: chuông nổi góc dưới-phải mọi trang (`layout.js`, không cần sửa từng trang HTML), 3 loại: thanh toán công nợ NCC, thanh toán công nợ khách hàng, sinh nhật đối tác (dùng `contacts.date_of_birth` có sẵn từ module "Đối tác", **không phải** `partners` — khác dự tính ban đầu lúc tạm dừng). Migration `027`: `contacts.hobby` (mới, theo yêu cầu), `notification_settings` (singleton, bật/tắt từng loại + `birthday_reminder_days` dạng CSV nhiều mốc, vd "3,1,0"), `notifications` (log, có `dedupe_key` UNIQUE tránh trùng), `notification_reads` (đã đọc riêng từng user). `backend/services/notification.service.js` mới — quét sinh nhật chạy **mỗi lần gọi** `GET /notifications`/`unread-count` (không gate theo "đã quét trong ngày" như dự tính ban đầu — phát hiện qua test: gate đó bỏ lỡ đối tác vừa thêm sau khi đã quét xong hôm đó; dedupe_key đã đủ an toàn chống trùng nên bỏ gate). Trang `notification-settings.html` (menu Cấu hình) cho Admin bật/tắt + quản lý danh sách mốc nhắc (thêm/xóa từng mốc). Card "Sinh nhật trong tháng" trên `dashboard.html` (mở cho mọi tài khoản qua route riêng `GET /contacts/birthdays-this-month`, không đòi quyền `doi_tac` — khác GET danh sách đầy đủ), đỏ khi còn ≤3 ngày, đã thu gọn mỗi dòng còn ~30px (tên+ngày sinh chung 1 dòng) sau phản hồi người dùng "quá to". "Realtime" là **giả lập qua polling 20s** (không có WebSocket) — phát hiện dòng thông báo mới thì tự hiện popup toast 4 giây ngay trên chuông. **2 lỗi CSS `[hidden]` bị `display` đè phát hiện khi test** (cùng loại lỗi đã gặp nhiều lần trong dự án) trên `.notification-panel`/`.notification-badge` — đã sửa bằng `:not([hidden])`. Test qua API (curl: dedupe đúng, hook thanh toán tạo đúng thông báo) + trình duyệt thật (CDP thô: toast xuất hiện đúng vị trí/nội dung/tự ẩn, panel thu gọn đúng, cấu hình lưu/tải lại đúng). Trường "Sở thích" cũng thêm vào `contacts.html`/`contact-detail.html` cùng migration này.
- ✅ **Module "Đối tác" (2026-08-05, ngoài phase, theo yêu cầu người dùng)**: danh bạ liên hệ cá nhân — Họ và tên/SĐT/Địa chỉ/Nghề nghiệp/Ngày sinh/Ghi chú/Sở thích — **hoàn toàn tách biệt** với "Nhà cung cấp"/"Khách hàng" (bảng `partners` không đổi gì). Migration `026` (bảng `contacts` độc lập, không FK tới bảng nào khác ngoài `created_by`), module quyền mới `doi_tac` (`backend/config/modules.js` — tự động xuất hiện ở trang "Vai trò", không cần sửa gì thêm nhờ cơ chế đã có từ trước). Thêm/sửa: quyền module `doi_tac`; **xóa: chỉ Admin** (`is_protected`), khác nguyên tắc thường của dự án là quyền module áp dụng đều mọi hành động — người dùng yêu cầu rõ ràng riêng cho xóa. Trang mới `contacts.html`/`.js` (list + modal, rập khuôn `warranties.html`) + `contact-detail.html`/`.js` (xem chi tiết, rập khuôn `customer-detail.html`, bổ sung theo yêu cầu người dùng ngay sau bản đầu), nhóm nav mới "Đối tác". Test qua API + trình duyệt thật, không lỗi console. Đã cập nhật `docs/PRD.md` (mục 4.13 mới), `docs/erd.mermaid`.
- ✅ **Sửa trang Báo cáo: phân trang bảng tồn kho + số tiền không xuống dòng + khớp chiều cao khung/biểu đồ (2026-08-05, ngoài phase, theo phản hồi người dùng kèm ảnh chụp)**: bảng tồn kho giới hạn 15 dòng/trang, có nút Trước/Sau (`.pagination`, component mới — đã dùng skill `ui-ux-pro-max`); số tiền lớn ở khung "Mua/Bán hàng tháng này" hết bị nhảy dòng (giảm cỡ chữ + `white-space:nowrap` + `ellipsis` dự phòng cho giá trị cực lớn sau này); khung số tiền và biểu đồ cột giờ luôn khớp chiều cao tuyệt đối — sau 3 lần thử CSS Grid/Flexbox stretch đều thất bại (do `.report-chart-svg` cũ dùng `height:auto` phụ thuộc bề rộng màn hình, xem `docs/DECISIONS.md`), đã đổi SVG sang chiều cao **cố định** (200px) rồi đặt `min-height` cố định tương ứng cho khung số tiền — đơn giản và triệt để hơn.
- ✅ **Module "Quản lý dự án" — Đợt 5/5: Báo cáo dự án (2026-08-05, tùy chọn, đợt cuối cùng theo kế hoạch gốc)**: section mới "Dự án đang hoạt động" trong `reports.html` — 4 thẻ tổng hợp (dự án đang hoạt động/trễ tiến độ/tổng còn phải thu/dự án vượt dự toán vật tư) + bảng chi tiết từng dự án, cột "Vật tư" bấm vào điều hướng thẳng sang đúng tab "Vật tư" của dự án đó (`project-detail.js` nay hỗ trợ mở sẵn tab qua URL `?tab=...`). API mới `GET /api/reports/projects` tái dùng nguyên các hàm tính tiến độ/trễ tiến độ/công nợ/vật tư đã có từ Đợt 1-4, không tự tính lại theo cách khác. Đã hỏi 3 câu chốt bố cục/phạm vi trước khi code. Test qua API + trình duyệt thật, không lỗi console. **Đến đây module "Quản lý dự án" đã hoàn thành trọn vẹn cả 5/5 đợt theo kế hoạch gốc.**
- ✅ **Định dạng dấu chấm phân cách hàng nghìn ngay trong ô nhập số tiền, toàn hệ thống (2026-08-05, ngoài phase, theo yêu cầu người dùng)**: `frontend/assets/money-input.js` (mới, dùng chung) — 11 trường tiền trên 9 trang (Sản phẩm, Đơn giá dòng phiếu nhập/xuất, Giá trị hợp đồng dự án, Đợt thanh toán/Phát sinh dự án, Hạn mức công nợ, Sổ quỹ, Công nợ NCC/Khách hàng) đổi từ `type="number"` sang `type="text" class="money-input"`, tự hiện dấu chấm khi gõ (đồng bộ `toLocaleString('vi-VN')` đã dùng ở mọi nơi hiển thị số tiền). Xem chi tiết `docs/DECISIONS.md`/`docs/DESIGN-SYSTEM.md`. Đã test qua trình duyệt thật (CDP thô, gõ ký tự thật) toàn bộ 11 trường + dòng phiếu nhập (rủi ro cao nhất vì có tính "Thành tiền" trực tiếp), không lỗi console.

Phase 1, 1.5, 1.6, 2 (Kho), 3 (Công nợ), **4 (In phiếu & Báo cáo) đã hoàn thành**. Sau đó có thêm nhiều đợt chỉnh sửa/mở rộng ngoài phase (Báo cáo, In phiếu, tách Khách hàng, Bảo hành, Tổng quan, tìm kiếm theo SĐT, module Sổ quỹ, Import/Export Excel sản phẩm — xem mục dưới). **Phase 5 (Vận hành & Go-live) đang làm** — phần code/cấu hình độc lập máy chủ đã xong, phần gắn với máy chủ thật (IP tĩnh, PM2 startup, go-live) chờ triển khai lên đúng máy chủ. **Module "Quản lý dự án" đã code xong cả 4 đợt theo kế hoạch đã chốt (Đợt 1→4, hoàn thành 2026-08-04)** + 1 đợt bổ sung ngoài kế hoạch gốc (gộp tab Công việc vào tab Giai đoạn) — chỉ còn Đợt 5 (Báo cáo dự án, tùy chọn) chưa làm, xem mục "Việc cần làm tiếp theo" ở cuối file.

- ✅ **Module "Quản lý dự án" — Đợt 4/4: Đợt thanh toán, Công nợ dự án & Phát sinh (2026-08-04, đợt cuối cùng theo kế hoạch)**: migration `025` (`project_payment_milestones`, `project_variations`, `debt_ledger.milestone_id`); `projectMilestones.routes.js`/`projectVariations.routes.js` mới (CRUD, trạng thái đợt thanh toán suy ra: Chưa thu/Thu một phần/Đã thu đủ/Quá hạn); `projects.routes.js` bổ sung filter `?partner_id=` + `contract_value_actual`/`debt_summary` ở `GET /:id`; `debt.service.js#recordPayment()`/`recordDebtAdjustment()` nhận thêm `projectId`/`milestoneId` (validate dự án thuộc đúng đối tác, đợt thuộc đúng dự án). Frontend: 2 tab mới trên trang dự án ("Thanh toán & Công nợ" — số liệu + đợt thanh toán + nút "Ghi nhận đã thu" điều hướng sang `customer-debts.html` khóa sẵn Khách hàng/Dự án/Đợt; "Phát sinh" — CRUD chi phí/vấn đề); `customer-debts.html`/`.js` thêm select "Dự án"/"Đợt thanh toán" trên form thanh toán (chỉ "Dự án" ở form điều chỉnh), không đụng `debts.html` NCC. Test qua API (2 dự án cùng khách hàng cách ly đúng số liệu, chặn đúng khi gán sai dự án/đợt) và trình duyệt thật (CDP thô, luồng đầy đủ từ bấm "Ghi nhận đã thu" đến số liệu cập nhật đúng), không lỗi console. **Đến đây module "Quản lý dự án" đã hoàn thành đủ 4 đợt theo kế hoạch gốc.**
- ✅ **Module "Quản lý dự án" — Đợt 3/4: Vật tư & gắn dự án vào phiếu nhập/xuất (2026-08-04, đợt nhạy cảm nhất vì sửa vào transaction tạo phiếu nhập/xuất đang chạy thật)**: migration `024` (`project_material_plan` + `ADD COLUMN project_id` nullable cho `stock_receipts`/`stock_issues`/`debt_ledger`); `stockReceipt.service.js`/`stockIssue.service.js` nhận thêm `projectId` (validate tồn tại trong transaction, ghi lên phiếu, truyền xuống `recordDebtFromDocument()`) — chỉ thêm 1 trường ghi kèm, không đụng logic tồn kho/giá vốn/ghi nợ hiện có; `projectMaterials.routes.js` mới (`/:id/materials`, cùng pattern `projectTasks.routes.js`) — CRUD dự toán + tính "Đã xuất"/"Còn lại" on-the-fly (`SUM` xuất trừ `SUM` nhập cùng dự án, đúng nguyên tắc "trả vật tư thừa về kho qua phiếu nhập phải trừ vào Đã xuất"). Frontend: dropdown "Dự án" (không bắt buộc) trên form lập phiếu nhập/xuất, dòng "Dự án" trong modal chi tiết phiếu, dòng "Công trình: …" tự ẩn trên phiếu in, tab "Vật tư" mới trên trang chi tiết dự án (bảng dự toán + bảng phiếu đã gắn). Nhân tiện hiện thực hóa luôn việc chặn xóa dự án đã có phiếu/công nợ gắn vào (`project.service.js#deleteProject()` — đã ghi sẵn comment "sẽ bổ sung ở Đợt 3/4" từ Đợt 1, đúng PRD 4.12, không phải phát sinh ngoài kế hoạch). Test qua API (curl, dự án test riêng `DA000002` không đụng dữ liệu thật "Villa Kỳ Duyên": phiếu có/không gắn dự án đều đúng, dự án không tồn tại bị chặn + rollback đúng, `debt_ledger.project_id` xác nhận trực tiếp qua DB, xóa dự án có phiếu bị chặn đúng) và trình duyệt thật (Chrome headless CDP thô — môi trường không có `chromium-cli`/Playwright, viết script Node dùng `WebSocket` built-in của Node 22+; sự cố nhỏ tự sửa ngay: nhiều `Runtime.evaluate` liên tiếp dùng lại tên biến `const` bị lỗi trùng khai báo do chung 1 global scope, sửa bằng cách bọc mỗi lệnh trong `{ }` để tự tách block scope): lập phiếu qua UI thật có gắn dự án, xem chi tiết phiếu, xem phiếu in, thêm dự toán vật tư qua UI — số liệu "Đã xuất"/"Còn lại" khớp đúng công thức, không lỗi console. Dữ liệu test giữ lại theo nguyên tắc ledger (nay còn bị chính cơ chế mới chặn xóa dự án), chấp nhận để lại.
- ✅ **Module "Quản lý dự án" — bỏ tab "Công việc" riêng, gộp vào tab "Giai đoạn" + ngày thực tế nhập tay cho công việc (2026-08-04, ngoài phase, theo yêu cầu người dùng, đã hỏi 3 câu chốt phạm vi trước khi code)**: bấm vào 1 dòng giai đoạn (bảng "Danh sách giai đoạn") sẽ xổ ra ngay bảng công việc con của giai đoạn đó — mỗi công việc có 2 ô "Bắt đầu thực tế"/"Kết thúc thực tế" nhập tay + chọn Trạng thái + nút "Lưu" cập nhật ngay không cần mở modal (icon "Sửa" vẫn mở modal đầy đủ). Migration `023_project_task_actual_dates.sql`: `project_tasks.actual_start_date`/`actual_end_date` **thay thế hoàn toàn** `completed_at` tự động của Đợt 2 (giữ nguyên cột cũ, không xóa) — cảnh báo trễ tiến độ công việc nay so `due_date` với `actual_end_date`. Biểu đồ Gantt dùng chung trạng thái mở rộng với bảng (`expandedPhaseIds`) — bấm giai đoạn trên Gantt cũng xổ ra danh sách công việc dạng text ngắn gọn (không vẽ thêm thanh SVG riêng). **Vì migration `023` này ngoài kế hoạch ban đầu, đã lùi số các migration kế hoạch còn lại của Đợt 3/4**: `project_material_plan` từ `023`→`024`, `project_payment_milestones`/`project_variations` từ `024`→`025` — xem `docs/Plan.md`. Test qua API + trình duyệt thật (CDP thô, 3 script), không lỗi console. **Sự cố phát hiện + xử lý trước khi code**: server Node đang chạy dùng code cũ hơn lần sửa gần nhất (không tự nạp lại khi file thay đổi) khiến cảnh báo trễ tiến độ (đã sửa đúng từ trước) không hiển thị gì — đã restart server, xác nhận đúng. Dữ liệu thật "Villa Kỳ Duyên" bị đổi tạm 2 lần khi test qua trình duyệt đã khôi phục ngay. **Sửa thêm 2 lỗi hiển thị sau phản hồi "nền bị trắng" của người dùng**: `display:flex` đặt thẳng lên `<td>` hành động làm bảng tính sai độ rộng cột (chỉ ~24px, nút tràn ra ngoài ô) — sửa bằng cách bọc nút trong `<div>` riêng bên trong ô; `.phase-tasks-row td` (selector hậu duệ) vô tình áp nền muted lên cả bảng con lồng bên trong — sửa thành `.phase-tasks-row > td`. Lần này điều tra bằng computed style + tọa độ đo qua CDP thay vì đoán từ ảnh chụp, test hồi quy dùng giai đoạn trống thay vì dữ liệu thật. Trong lúc điều tra phát hiện 2 công việc thật bị mất khỏi dự án — người dùng xác nhận chính họ đã xóa, không phải lỗi.
- ✅ **Module "Quản lý dự án" — Đợt 2/4: Công việc & Timeline (2026-08-04, ngoài phase, theo yêu cầu người dùng)**: mỗi công việc thuộc 1 giai đoạn cụ thể, người phụ trách chỉ chọn được trong danh sách người tham gia dự án — % tiến độ giai đoạn/dự án giờ tự tính đúng từ tỷ lệ công việc hoàn thành (hàm `getProjectProgress()` đã viết sẵn logic từ Đợt 1, chỉ cần bảng `project_tasks` tồn tại là chạy đúng ngay, không cần sửa thêm).
  - Migration `022_project_tasks.sql`: `project_tasks` (chỉ lưu `phase_id`, không lưu `project_id` trùng lặp — lấy dự án qua JOIN giai đoạn để tránh lệch dữ liệu khi chuyển việc sang giai đoạn khác).
  - `backend/routes/projectTasks.routes.js` (router `mergeParams:true`, **lồng trực tiếp vào `projects.routes.js`** tại `/:id/tasks` thay vì mount riêng ở `server.js` — tránh rủi ro 2 router cùng tiền tố `/api/projects` khớp route sai thứ tự): CRUD, validate giai đoạn thuộc đúng dự án, validate người phụ trách thuộc đúng danh sách tham gia; `completed_at` tự động gán/xóa theo trạng thái (không nhập tay).
  - `projects.routes.js`: `getPhases()` bổ sung `progress_percent` riêng từng giai đoạn; `project.service.js`: `deleteProject()` bổ sung chặn khi dự án đã có công việc.
  - Frontend tab "Công việc" mới (`project-detail.html`/`.js`): lọc theo giai đoạn, modal thêm/sửa/xóa. Gantt (Đợt 1) nay có phần tô đậm bên trong thanh thể hiện % hoàn thành + nhãn số; bảng "Danh sách giai đoạn" thêm cột "Tiến độ".
  - Test qua API + trình duyệt thật (CDP thô, viết lại script test riêng cho Đợt 2). **Phát hiện + sửa 1 lỗi hiển thị thật**: 2 nhãn tháng liên tiếp trên Gantt đè chữ lên nhau khi cùng bị ép sát mép trái (hệ quả phụ của cách sửa lỗi Đợt 1) — đã sửa bằng cách bỏ nhãn nào cách nhãn liền trước dưới 55px, vẫn giữ đường kẻ phân tháng. Dữ liệu test đã xóa sạch sau khi xong.
- ✅ **Sửa 4 lỗi module "Quản lý dự án" phát hiện khi người dùng tự test qua UI thật (2026-08-04)**: (1) tạo dự án chọn "Đang thực hiện" nhưng danh sách hiện "Chuẩn bị" — `createProject()` thiếu hẳn cột `status` trong câu INSERT; (2) "Thêm giai đoạn" điền Trạng thái + ngày thực tế nhưng lưu vẫn "Chưa bắt đầu" — route tạo giai đoạn mới bỏ qua hoàn toàn 3 trường đó (khác route sửa giai đoạn vẫn đúng); (3) nút "Thêm giai đoạn"/"Thêm công việc" trông như bị lỗi — chưa từng gán icon+chữ qua JS, hiện ra chỉ là khối gradient rỗng; (4) combobox lọc trạng thái/giai đoạn không đúng style thiết kế — `<select>` trần chưa có class CSS nào áp dụng, đã thêm `.filter-select` dùng chung. **Sự cố phát sinh khi điều tra**: gõ tiếng Việt có dấu trực tiếp vào `curl -d` qua Git Bash (lỗi đã biết trước) lần này ghi đè lên dữ liệu thật của người dùng (dự án "Villa Kỳ Duyên") — đã khôi phục đúng nguyên trạng ngay. Nhân tiện rà và sửa toàn bộ ~20 chuỗi thông báo lỗi tự viết cho module Dự án sang có dấu tiếng Việt đầy đủ theo phản hồi trực tiếp của người dùng.
- ✅ **Cảnh báo "Trễ tiến độ" cho giai đoạn + công việc (2026-08-04, theo yêu cầu người dùng, đã hỏi 2 câu chốt phạm vi trước khi code)**: tính cả "đang trễ" (chưa xong, đã quá hạn dự kiến so với hôm nay) lẫn "xong trễ" (đã xong nhưng ngày thực tế trễ hơn dự kiến) — `computeDelay()` mới trong `project.service.js`, mốc "hôm nay" theo giờ VN cố định UTC+7 (nhất quán với module Sổ quỹ). Hiển thị bằng nhãn cảnh báo đỏ kèm icon dưới badge Trạng thái ở cả bảng Giai đoạn lẫn bảng Công việc — không đổi màu Gantt (đúng lựa chọn người dùng). Không lưu, luôn tính lại theo thời điểm xem. **Phát hiện + sửa ngay 1 lỗi thật** (người dùng hỏi "tại sao tạo lịch trễ vẫn không báo"): giai đoạn đánh dấu "Hoàn thành" qua dropdown nhưng chưa điền `actual_end` (trường nhập tay riêng biệt, không tự động như `completed_at` của công việc) → không có gì để so sánh nên im lặng báo không trễ dù đã quá hạn rất lâu — đã sửa fallback dùng "hôm nay" khi thiếu `actual_end`.

- ✅ **Module "Quản lý dự án" — Đợt 1/4: Nền tảng dự án (2026-08-04, ngoài phase, theo yêu cầu người dùng)**: quản trị toàn bộ quá trình dự án theo giai đoạn/thời gian/khách hàng/công trình. Đã hỏi và chốt **14 quyết định** qua 3 vòng `AskUserQuestion` trước khi lên kế hoạch, người dùng duyệt kế hoạch trước khi code — chi tiết đầy đủ `docs/DECISIONS.md` mục 2026-08-04, nghiệp vụ `docs/PRD.md` mục 4.12. **Nguyên tắc nền tảng quan trọng nhất** (áp dụng xuyên suốt cả 4 đợt, chưa đụng tới ở Đợt 1 nhưng quyết định kiến trúc bảng đã chuẩn bị sẵn): sổ cái công nợ **vẫn thuộc về khách hàng**, dự án không có sổ riêng — sẽ chỉ gắn thêm nhãn `project_id` lên dòng `debt_ledger` (Đợt 3) để lọc, tái dùng nguyên form thanh toán Công nợ khách hàng (Đợt 4) — **không** dựng lại bảng `debt_ledger` (cột `reference_type` có `CHECK` mà SQLite không `ALTER` được).
  - Migration `021_projects.sql`: `project_phase_templates` (danh mục "Giai đoạn mẫu", seed 6 giai đoạn: Khảo sát → Thiết kế → Chuẩn bị vật tư → Thi công → Nghiệm thu → Bàn giao & Bảo hành), `projects` (mã tự sinh `DA000001...`, gắn 1 khách hàng bắt buộc, số/ngày ký hợp đồng, giá trị hợp đồng, ngày bắt đầu/hoàn thành dự kiến/thực tế, trạng thái, người phụ trách), `project_members` (người tham gia kèm vai trò tự do), `project_phases` (giai đoạn của từng dự án, copy từ mẫu lúc tạo rồi **tách rời hoàn toàn**). **Không seed quyền `du_an` cho vai trò mặc định nào** (khác `so_quy` rõ ràng thuộc Kế toán — module này liên quan cả Kho/Công nợ/điều phối nhân sự, để Admin tự cấp qua trang Vai trò).
  - `backend/services/project.service.js`: sinh mã, validate khách hàng/người tham gia, copy giai đoạn mẫu (transaction), tính % tiến độ (trả `null` — hiện "-" — cho tới khi có `project_tasks` ở Đợt 2), chặn xóa dự án đã có giai đoạn "đang làm"/"hoàn thành" (chỉ cho chuyển trạng thái "Hủy").
  - `backend/routes/projectPhaseTemplates.routes.js` (CRUD, GET mở/ghi quyền `cau_hinh`), `projects.routes.js` (CRUD dự án + sub-resource `/:id/phases`, toàn bộ quyền `du_an`).
  - Frontend mới: `project-phase-templates.html`/`.js` (menu Cấu hình), `projects.html`/`.js` (danh sách + modal thêm/sửa kèm chọn nhiều người tham gia + vai trò tự do), `project-detail.html`/`.js` (tab Tổng quan + Giai đoạn). `layout.js` thêm nhóm nav "Dự án" (sau "Khách hàng", trước "Quỹ"), `icons.js` thêm icon `briefcase`.
  - **2 pattern giao diện hoàn toàn mới trong dự án**, thiết kế qua skill `ui-ux-pro-max`, đã cập nhật `docs/DESIGN-SYSTEM.md`: (1) **trang chi tiết dạng tab** (`project-detail.html` — mọi trang chi tiết trước đây đều cuộn dọc 1 mạch), (2) **biểu đồ Gantt tự vẽ bằng SVG tay** (không dùng thư viện ngoài, nhất quán quyết định đã chốt ở trang Báo cáo).
  - Test qua API (curl, dùng file JSON UTF-8 thật thay vì gõ tiếng Việt trực tiếp vào `-d` — phát hiện gõ trực tiếp qua git-bash làm hỏng encoding, không phải lỗi ứng dụng) + trình duyệt thật (Chrome headless điều khiển bằng CDP thô tự viết, vì môi trường không có sẵn `chromium-cli`/Playwright). **Phát hiện + sửa 1 lỗi UX khi test qua trình duyệt**: giai đoạn có ngày bắt đầu trùng đúng mép trái vùng vẽ Gantt không hiện nhãn tháng tham chiếu nào (tháng chứa mốc bắt đầu bị coi là "ngoài vùng vẽ" nên bỏ qua) — đã sửa bằng cách đệm 3 ngày mỗi bên trước khi tính tỷ lệ, đồng thời ép hiện nhãn tháng đầu tiên tối thiểu tại mép trái. Dữ liệu test đã xóa sạch sau khi xong.

- ✅ **Trường "Người phụ trách" cho Khách hàng (2026-08-03, ngoài phase, theo yêu cầu người dùng)**: form thêm/sửa khách hàng (`customers.html`) có thêm ô tìm-và-chọn 1 tài khoản người dùng (combobox gõ ký tự gợi ý, tái dùng đúng pattern `.combobox` đã có ở "Điều chỉnh cho phiếu"), không bắt buộc. Migration `020` (`partners.assigned_user_id`, FK tới `users`, áp dụng chung cột cho cả bảng `partners` nhưng chỉ hiện trường này ở `customers.html` theo đúng phạm vi yêu cầu). Route mới `GET /api/partners/staff` (danh sách tài khoản đang hoạt động, không đòi quyền `nguoi_dung` như `GET /api/users` — cùng cách giải quyết đã dùng cho `GET /api/cash-vouchers/staff` ở module Sổ quỹ) vì trang Khách hàng chỉ đòi quyền `cong_no`. Đã test qua API (tạo/sửa với `assigned_user_id` hợp lệ/không hợp lệ) và trình duyệt thật (gõ gợi ý, chọn, lưu, mở sửa lại xác nhận điền đúng tên đã chọn), không lỗi console. **Phạm vi đã thu hẹp theo yêu cầu người dùng lúc trao đổi**: ban đầu còn có ý định thêm nút "Lưu tạm" (lưu nháp) cho phiếu nhập/xuất/bảo hành — người dùng yêu cầu bỏ hẳn phần đó, chỉ làm "Người phụ trách".
- ✅ **Sửa lỗi trang "Vai trò" thiếu module "Sổ quỹ" (2026-08-03, ngoài phase, theo phản hồi người dùng)**: `frontend/assets/roles.js` từng hardcode 1 danh sách module riêng, quên cập nhật khi thêm `so_quy` (module Sổ quỹ) — khiến checkbox "Sổ quỹ" không hiện trong modal "Sửa vai trò", nên sửa+lưu vai trò Kế toán sẽ vô tình xóa mất quyền đó. Đã sửa tận gốc: `backend/config/modules.js` bổ sung `MODULE_LABELS`, route mới `GET /api/roles/modules` làm nguồn duy nhất, `roles.js` bỏ hardcode và gọi API này thay thế — từ nay thêm module mới chỉ cần sửa 1 chỗ. Đã test qua API + trình duyệt thật (tái hiện đúng thao tác cũ: mở sửa Kế toán, lưu ngay, xác nhận không còn mất quyền).
- ✅ **Đổi logo thương hiệu "NEXA One" (2026-08-03, ngoài phase, theo yêu cầu người dùng)**: thay icon/logo mặc định bằng bộ nhận diện thương hiệu thật ở 3 vị trí — trang đăng nhập (`login.html`, logo đầy đủ kèm slogan "Mọi dữ liệu. Một nền tảng."), sidebar mọi trang (`layout.js`, icon + chữ "NEXA One" thay "Kho & Công nợ" cũ, tách riêng 2 ảnh icon/chữ để chữ tự ẩn đúng khi thu gọn sidebar), trang "Thông tin phần mềm" (`about.html`, logo đầy đủ thay khung icon gradient cũ). File gốc người dùng cung cấp không có bản icon-only tách riêng — đã dùng Pillow (Python) phân tích bounding box pixel để tự cắt gọn khoảng trắng/trong suốt thừa và tách icon/chữ thành từng file riêng, lưu tại `frontend/assets/images/` (thư mục mới, theo đúng quy ước host offline nhị phân sẵn có như `frontend/assets/fonts/`). **Phát hiện + sửa 1 lỗi phát sinh**: sidebar thu gọn (60px) làm icon logo biến mất hoàn toàn (bị flexbox ép về width:0 do `.sidebar-brand` có `overflow:hidden` khiến automatic min-width = 0, trong khi hàng ngang icon+nút thu gọn không đủ 32px khả dụng) — sửa bằng cách xếp dọc icon/nút thu gọn khi ở trạng thái collapsed thay vì chen chung 1 hàng. Đã test qua trình duyệt thật (Chrome headless CDP thô, chụp ảnh cả 2 trạng thái sidebar + trang đăng nhập + trang Thông tin phần mềm), không lỗi console. **Chỉnh sửa thêm sau phản hồi người dùng (2026-08-04)**: (1) logo trang đăng nhập chưa đủ nét — đổi nguồn cắt từ bản độ phân giải thấp (707×353) sang bản độ phân giải cao (1774×887, nền trắng gần khớp `--color-surface`), ảnh mới `nexa-one-logo.png` 1518×338; (2) dòng "Quản lý Kho & Công nợ" quá sát logo — tăng margin-top của `<h1>` từ 16px lên 26px (chỉ áp dụng `login.html`, không ảnh hưởng `setup.html`); (3) người dùng gửi thêm 1 bản logo dạng SVG để đánh giá thay thế PNG — đã kiểm tra kỹ (dựng thử qua trình duyệt, so màu từng điểm ảnh) và phát hiện file có nền trắng đục che kín canvas (không trong suốt thật) + dữ liệu path/gradient bị lặp nguyên khối 4 lần (~80KB dư thừa) + màu xanh thương hiệu nằm ở các path chi tiết tách rời khỏi path chính (rủi ro mất màu nếu trích xuất không cẩn thận) — đã báo cáo phát hiện và hỏi lại, **người dùng chọn giữ nguyên bộ PNG hiện tại**, không dùng SVG này.
- ✅ **Import/Export Excel cho Sản phẩm (2026-08-02, ngoài phase, theo yêu cầu người dùng)**: trang Sản phẩm có thêm nút "Nhập Excel" (modal: tải file mẫu `.xlsx` có sẵn sheet "Hướng dẫn", chọn file, báo lỗi rõ dòng nào/lỗi gì nếu có, **toàn bộ file phải hợp lệ mới nhập** — không nhập một phần) và "Xuất Excel" (**xuất đúng danh sách đang hiển thị trên giao diện**, tôn trọng ô tìm kiếm hiện tại, không phải toàn bộ danh mục). 3 quyết định nghiệp vụ đã hỏi và chốt trước khi code (mã trùng → báo lỗi bỏ qua chứ không upsert; còn lỗi thì không nhập gì cả; export theo đúng danh sách đang hiển thị) — xem `docs/DECISIONS.md`. Backend dùng `exceljs` (đọc/ghi `.xlsx`) + `multer` (nhận file upload, giới hạn 5MB, lưu bộ nhớ tạm không ghi đĩa) — 2 dependency mới, thêm qua `npm install`. 3 route mới trong `products.routes.js`: `GET /import-template`, `POST /import`, `POST /export` (đặt trước `GET /:id` vì Express khớp `/:id` với mọi đoạn đường dẫn 1 từ, kể cả `import-template`, nếu đăng ký sau). Validate import mirror đúng rule của form thêm/sửa sản phẩm hiện có (mã/tên/đơn vị/giá bán bắt buộc), cộng thêm kiểm tra số hợp lệ cho Giá vốn/Ngưỡng cảnh báo (khác form thường vốn âm thầm quy về 0 nếu nhập rác — bulk import dễ có lỗi gõ nhầm hơn nên báo rõ thay vì im lặng). Test qua API (curl: file lỗi bị chặn đúng + báo đúng dòng/lỗi, file hợp lệ nhập đúng, mã trùng DB bị chặn, sai định dạng/thiếu file bị chặn, phân quyền `kho` chặn đúng nhưng export vẫn mở cho mọi tài khoản đã đăng nhập giống `GET /products`) và qua trình duyệt thật (Chrome headless điều khiển CDP thô, có chụp ảnh màn hình): mở modal, upload file lỗi → bảng lỗi hiển thị đúng, upload file hợp lệ → thông báo thành công + danh sách tự làm mới, bấm "Xuất Excel" tải file thật về máy đúng nội dung đang hiển thị, không lỗi console. Dữ liệu test tạo ra trong lúc test đã xóa sạch sau khi xong.
- ✅ **Module "Sổ quỹ" (2026-08-02, ngoài phase, theo yêu cầu người dùng)**: quản lý dòng tiền quỹ tiền mặt — phiếu thu/phiếu chi (mã tự sinh `PT`/`PC` riêng từng loại), thanh tổng hợp Quỹ đầu kỳ/Tổng thu/Tổng chi/Tồn quỹ theo tháng (mặc định tháng hiện tại, chọn Tháng/Năm để xem tháng cũ), danh mục "Loại thu chi" quản lý riêng (tạo nhanh được ngay trên modal lập phiếu). **Hoàn toàn độc lập với module Công nợ** (không ghi `debt_ledger`), **Quỹ đầu kỳ nhập 1 lần tự động cộng dồn** qua các tháng, **phiếu không sửa được chỉ tạo/xóa cứng** — 3 quyết định kiến trúc đã hỏi và chốt với người dùng trước khi code, xem `docs/DECISIONS.md`. Migration `019` (`cash_book_settings`/`cash_categories`/`cash_vouchers`), module quyền mới `so_quy` (seed cho vai trò Kế toán), nhóm nav mới "Quỹ". **Điểm kỹ thuật quan trọng**: mốc "tháng hiện tại" tính theo giờ VN (UTC+7 cố định hardcode), không phải UTC thô hay giờ server — đã test kỹ ranh giới tháng qua trình duyệt thật + script Node trực tiếp gọi service. Sau khi demo, đã sửa thêm theo phản hồi người dùng: nút đầu trang bị lệch dòng (do bọc `<input type="month">` trong `.form-field` vốn có `margin-bottom`) → đổi hẳn sang 2 `<select>` "Tháng"/"Năm" tiếng Việt (vì `<input type="month">` hiển thị theo locale trình duyệt, không ép được tiếng Việt); bổ sung nút "+ Tạo loại mới" ngay trên modal lập phiếu thu/chi để không phải rời sang trang riêng.
- ✅ **Đóng gói bản portable + thiết lập lần đầu qua giao diện + tự khởi động cùng Windows (2026-08-01, ngoài phase)**: `backend/server.js` tự chạy migration khi khởi động; `setup.routes.js`/`setup.html` mới cho phép tạo tài khoản Admin đầu tiên qua giao diện (thay `npm run seed:admin`, tự khoá sau khi có tài khoản); `scripts/build-portable.js` (`npm run build:portable`) đóng gói thành thư mục `dist/` tự chứa `node.exe` + `start.bat`, không cần cài Node trên máy đích; `install-autostart.ps1`/`uninstall-autostart.ps1` đăng ký Task Scheduler tự khởi động cùng Windows. **Đã thử `@yao-pkg/pkg` để đóng gói thành 1 file `.exe` duy nhất trước, thất bại vì crash native-addon `better-sqlite3` không sửa được** — đổi sang thư mục portable (đáng tin cậy hơn, đánh đổi không che giấu mã nguồn backend) — chi tiết đầy đủ `docs/DECISIONS.md`. Đã test qua trình duyệt thật (bản portable, database rỗng): tự tạo DB → thiết lập lần đầu → đăng nhập → dashboard đúng; đã khôi phục lại dữ liệu dev/demo gốc sau test. **Chưa làm**: chạy `install-autostart.ps1` thật trên máy chủ chính thức (máy đang thao tác vẫn là máy dev).
- ✅ **Bản quyền + "Thông tin phần mềm" (2026-08-01, ngoài phase)**: footer sidebar mọi trang có dòng bản quyền, trang `about.html` mới (menu Cấu hình, mở cho mọi tài khoản). Nhân tiện sửa 3 lỗi CSS: subtitle rớt chữ, sidebar tràn màn hình/icon biến mất khi thu gọn (đã thêm `height:100vh`/`position:sticky` cho sidebar + `flex-shrink:0` cho icon), `.stat-grid` dính `.data-table-wrap` ở trang Báo cáo.
- ✅ **Bắt đầu Phase 5 — Vận hành & Go-live (2026-08-01)**: đã làm phần độc lập với máy chủ cụ thể — `ecosystem.config.js` (PM2), migration `018` + `scripts/backup.js` (backup `data.db`, tự dọn bản cũ >14 ngày), UI cấu hình đường dẫn backup + nút "Backup ngay" trong "Cấu hình kho", tài liệu quy trình đầy đủ `docs/DEPLOY.md`. **Chưa làm** (cần đúng máy chủ thật, hiện đang ở máy dev): đặt IP tĩnh, chạy `pm2 start`/`pm2-startup install`, đặt Task Scheduler backup hàng ngày, go-live thật.
- ✅ **Làm lại trang Tổng quan (2026-08-01, ngoài phase)**: `dashboard.js` mới (trước đây chỉ là khung tĩnh, không có code gọi API) — lời chào động theo giờ hệ thống + họ tên tài khoản, 3 card Sản phẩm/Khách hàng/Nhà cung cấp lấy số liệu thật, thiết kế lại bằng skill `ui-ux-pro-max` (card bento bấm được, icon mặt trời/mặt trăng, khu vực "Truy cập nhanh" lọc theo quyền) — xem `docs/DESIGN-SYSTEM.md`.
- ✅ **Bảo hành (2026-08-01, ngoài phase, theo yêu cầu người dùng, đã chỉnh sửa lại theo phản hồi ngay sau đó)**: module mới thuộc menu Khách hàng — migration `017` (bảng `warranties`, chỉ gắn khách hàng), CRUD đầy đủ (`warranties.routes.js`), trang danh sách (`warranties.html`) kèm **modal** thêm mới/sửa (ban đầu làm 1 trang riêng `warranty-detail.html`, đã bỏ và chuyển thành popup theo đúng pattern chung của hệ thống sau phản hồi người dùng), tương tác 2 chiều Thời gian bảo hành ↔ Ngày hết hạn (`warranty-calc.js`). Trang mới `customer-detail.html` (trước đây chưa có) hiển thị **card Bảo hành theo mẫu tham khảo** (icon vuông màu theo trạng thái, tiêu đề/phụ đề, nhãn trạng thái đối diện số ngày còn lại). Xóa cứng chỉ Admin. Sửa thêm 3 lỗi CSS `[hidden]` bị ghi đè phát hiện khi test (`.empty-state`, `.page-header-actions`, `.page-header-actions .btn-secondary`) + lỗi 2 card "Thông tin công ty"/"Ghi chú in phiếu" dính sát nhau (thiếu margin).
- ✅ **Chỉnh sửa Báo cáo/In phiếu + tách Khách hàng (2026-08-01, ngoài phase, theo yêu cầu người dùng)**: trang Báo cáo đổi thứ tự (Công nợ lên đầu); phiếu in xuất kho thêm cột "Mã sản phẩm" + sửa lỗi ghi chú công ty không xuống dòng (thiếu class CSS); tách hẳn "Khách hàng" thành menu riêng khỏi "Đối tác/Công nợ" (nay chỉ còn Nhà cung cấp) — thêm danh mục "Loại khách hàng" (migration `015`, có hạn mức công nợ chỉ để cảnh báo, không chặn cứng). Chi tiết đầy đủ: `docs/CHANGELOG.md`, quyết định kiến trúc: `docs/DECISIONS.md`.
- ✅ Phase 1 → 3: xem chi tiết ở các mục cũ bên dưới, không đổi gì thêm phiên này.
- ✅ **Phase 4 — In phiếu xuất kho** (`print-issue.html`, trang độc lập không dùng sidebar): dùng `GET /api/stock-issues/:id` có sẵn (không cần API `/print` riêng như dự tính ban đầu), hiển thị đầy đủ thông tin công ty (tên, địa chỉ, điện thoại, MST, email, website, ngân hàng) + thông tin khách hàng + bảng sản phẩm (kèm chiết khấu) + tổng tiền + chữ ký. Nút "In phiếu" (`window.print()`) trên từng dòng ở `stock-issues.html`.
- ✅ **Ghi chú in phiếu cấu hình được** (`company_settings.print_note`, migration `014`): nội dung hiển thị dưới bảng kê trên phiếu in (điều kiện bảo hành, chính sách đổi trả...) — sửa được qua trang Thông tin công ty (textarea mới), không hardcode cứng trong code. Seed sẵn nội dung mẫu theo yêu cầu người dùng.
- ✅ **Phase 4 — Báo cáo** (`reports.html`, quyền `bao_cao`): 3 phần — (1) Tồn kho hiện tại (bảng + tổng giá trị tồn, giá vốn luôn tính bình quân gia quyền bất kể `costing_method` đang chọn), (2) Mua hàng/Bán hàng theo tháng (biểu đồ cột SVG tự vẽ tay, không dùng thư viện ngoài, kèm % tăng trưởng so với tháng trước), (3) Công nợ tổng hợp toàn hệ thống (tổng phải thu/phải trả). `backend/routes/reports.routes.js` tính trực tiếp từ bảng gốc, không lưu số tổng hợp riêng.

## Đã hoàn thành (tiếp — 2026-08-02, ngoài phase)

- **Tìm kiếm theo số điện thoại**: mở rộng ô tìm kiếm (vốn chỉ lọc theo tên) để lọc thêm theo số điện thoại trên 5 trang: Nhà cung cấp (`partners.html`), Công nợ NCC (`debts.html`), Khách hàng (`customers.html`), Công nợ khách hàng (`customer-debts.html`), Bảo hành (`warranties.html`) — chỉ đổi hàm lọc phía client (`getVisiblePartners`/`getVisibleSummary`/`getVisibleCustomers`/`getVisibleWarranties`, khớp theo tên HOẶC số điện thoại), không đổi API/backend vì `phone` đã có sẵn trong response của cả 3 route (`/partners`, `/debts/summary`, `/warranties`). Cập nhật placeholder ô tìm kiếm ghi rõ "...hoặc số điện thoại...". Test qua trình duyệt thật (Chrome headless điều khiển bằng CDP, có chụp ảnh màn hình xác nhận): gõ số điện thoại/1 đoạn số trên cả 5 trang đều lọc đúng về 1 dòng khớp, không lẫn dòng khác.

## Đã hoàn thành

- Tài liệu thiết kế + quản lý dự án: `docs/PRD.md`, `docs/Plan.md`, `docs/erd.mermaid`, `docs/CURRENT.md`, `docs/TASK.md`, `docs/CHANGELOG.md`, `docs/DECISIONS.md`, `docs/DESIGN-SYSTEM.md`, `docs/DEMO.md`, `.claude/docs/inventory-debt-ledger.md`.
- **Phase 1 — Nền tảng**: `package.json` (Express 5.x, better-sqlite3, bcrypt, express-session), migration runner + `001_init.sql`, script seed admin không hardcode credential, API `/api/auth/login|logout|me`, trang `frontend/login.html`.
- Hướng dẫn chạy demo tại `docs/DEMO.md`. Đã test thành công mô hình LAN thật (nhiều máy truy cập 1 máy chủ), kèm hướng dẫn mở Windows Firewall port 3000.
- **Design system** (`docs/DESIGN-SYSTEM.md`, tạo bằng skill `ui-ux-pro-max`): style Soft UI Evolution, màu xanh dương + accent, font Be Vietnam Pro/Open Sans host offline tại `frontend/assets/fonts/`, icon SVG dùng chung (`icons.js`).
- **Phase 1.5 — Quản trị người dùng & Phân quyền**: trang `frontend/users.html` (có thêm sửa/xóa tài khoản), khung điều hướng dùng chung `layout.js`/`icons.js`.
- **Phase 1.6 — toàn bộ (backend + frontend)**: vai trò động, cấu hình công ty/kho.
- **Phase 2 — Kho (hoàn thành)**: migration `005`-`010`, `costing.service.js`, `products.routes.js` mở rộng, `stockReceipts.routes.js`/`stockIssues.routes.js` (kèm cơ chế điều chỉnh bù trừ), frontend `products.html`/`product-detail.html`/`stock-receipts.html`/`stock-issues.html`, modal xem chi tiết phiếu nhập + phiếu xuất dùng chung (`receipt-detail.js`/`issue-detail.js`), combobox "Điều chỉnh cho phiếu" dùng chung (`adjustment.js`).
- **Phase 3 — Công nợ (hoàn thành)**: migration `011`-`012`, `debt.service.js`, `debts.routes.js`, `partners.routes.js` CRUD đầy đủ, frontend `partners.html`/`debts.html`, bật menu "Đối tác"/"Công nợ" trong `layout.js`.
- **Hoàn thiện Xuất kho + Công nợ (sau Phase 3)**: migration `013` (chiết khấu phiếu xuất), `stockIssue.service.js`/`stockIssues.routes.js` cập nhật (thời gian xuất, chiết khấu), `debts.routes.js` bổ sung `total_transacted`, frontend `stock-issues.html`/`.js` (thời gian xuất, hiển thị liên hệ khách hàng, chiết khấu), `debts.html`/`.js` (thẻ số liệu nổi bật), CSS `.stat-card-value--warning` + spacing `.setting-row`.
- **Phase 4 — In phiếu & Báo cáo (hoàn thành)**: migration `014` (`company_settings.print_note`), `backend/routes/reports.routes.js` (mới), frontend `print-issue.html`/`.js` (mới), `reports.html`/`.js` (mới, biểu đồ cột SVG tự vẽ + `.stat-delta`), `company-settings.html`/`.js` cập nhật (textarea ghi chú in phiếu), icon `printer` mới, bật menu "Báo cáo" trong `layout.js`.
- Tài khoản demo hiện có trong `data/data.db`: `admin` / `Demo@123456` (Admin), `thukho1` / `ThuKho@123` (Thủ kho), `khophu1` / `KhoPhu@123` (vai trò tự tạo, quyền `kho`+`cong_no`), `ketoan1` / `KeToan@123` (Kế toán). Có nhiều dữ liệu test tích lũy qua các phiên (phiếu nhập/xuất PN/PX..., vài đối tác test, vài dòng `debt_ledger` test) — không xóa được theo đúng nguyên tắc ledger, chấp nhận để lại. `company_settings` hiện có dữ liệu test đầy đủ (email/website/ngân hàng) để demo trang in phiếu.

## Đã hoàn thành (tiếp — 2026-08-01, ngoài phase)

- Migration `015_customer_categories.sql`: bảng `customer_categories` + `partners.category_id`.
- Backend: `customerCategories.routes.js` (mới), `partners.routes.js`/`debts.routes.js` cập nhật hỗ trợ `category_id`/cảnh báo hạn mức.
- Frontend mới: `customers.html`/`.js` (Khách hàng), `customer-debts.html`/`.js` (Công nợ khách hàng), `customer-categories.html`/`.js` (Loại khách hàng, menu Cấu hình).
- Frontend cập nhật: `partners.html`/`.js` (chỉ còn Nhà cung cấp), `debts.html`/`.js` (chỉ còn Công nợ NCC), `layout.js`/`icons.js` (menu mới, icon `truck`/`tag`), `reports.html` (Công nợ lên đầu), `print-issue.html`/`.js` (cột Mã sản phẩm, sửa lỗi xuống dòng ghi chú).
- Test qua trình duyệt thật: CRUD Loại khách hàng, gán loại cho khách hàng, xác nhận dữ liệu công nợ NCC cũ không mất sau migration, menu sidebar đúng cấu trúc mới.
- Sửa lỗi ô "Địa chỉ" không nhập được khi thêm nhanh khách hàng/NCC mới trên phiếu xuất/nhập kho (thiếu ô nhập + lỗi CSS `.form-row` đè lên thuộc tính `hidden`).
- **"Điều chỉnh công nợ"** (migration `016_debt_adjustment.sql`, `debt_ledger.is_adjustment`): sửa số dư công nợ sai (vd nhập nhầm giá vốn phiếu nhập) mà không sửa/xóa phiếu gốc — ghi thêm 1 dòng `debt_ledger` (tăng hoặc giảm tùy chọn), tùy chọn liên kết về đúng phiếu nhập/xuất bị sai (validate đúng đối tác), bắt buộc ghi lý do. Có ở cả trang Công nợ NCC và Công nợ khách hàng (nút đầu trang + icon từng dòng), badge riêng "Điều chỉnh" trong lịch sử để không nhầm với thanh toán thật.

## Chưa bắt đầu / chưa xong

- Phase 5 (Vận hành & Go-live) theo `docs/Plan.md` mục 4: cấu hình PM2, IP tĩnh, script backup `data.db`, `SESSION_SECRET` cố định.
- Module Bán hàng/POS đầy đủ — cần buổi trao đổi yêu cầu nghiệp vụ riêng. Hiện chỉ có khung menu trống (`sales-settings.html`).
- **Cần người dùng xác nhận lại**: nội dung "Ghi chú in phiếu" mặc định (seed từ migration `014`) có 1 dòng bị suy đoán lại vì ảnh mẫu gốc bị cắt ở lề ("Các sản phẩm thương hiệu khác bảo hành theo tiêu chuẩn của nhà sản xuất") — cần vào trang Thông tin công ty kiểm tra/sửa lại cho đúng nguyên văn nếu cần.
- In phiếu hiện chỉ có cho **phiếu xuất kho** (đúng phạm vi PRD 4.5 "In phiếu xuất") — chưa có mẫu in cho phiếu nhập kho, chưa có yêu cầu cụ thể. Từ 2026-08-05 đã có khung "Cấu hình mẫu in" tổng quát (`backend/config/printTemplateTokens.js`) — thêm mẫu in phiếu nhập kho sau này chỉ cần thêm 1 entry `type` mới + 1 trang render tương tự `print-issue.html`, không cần dựng lại cơ chế.

## Việc cần làm tiếp theo

**Hệ thống thông báo đã xong phiên bản đầu** (3 loại: thanh toán NCC/KH, sinh nhật đối tác) — các hướng mở rộng còn để ngỏ, chưa có yêu cầu cụ thể: thêm loại thông báo mới (dự án mới/phiếu xuất mới từng được nhắc tới ban đầu nhưng chưa làm), cho từng user tự bật/tắt riêng thay vì cấu hình chung toàn hệ thống.


**Module "Quản lý dự án" đã hoàn thành trọn vẹn cả 5/5 đợt theo kế hoạch gốc** (Đợt 1: nền tảng, Đợt 2: Công việc & Timeline, Đợt 3: Vật tư & gắn dự án vào phiếu, Đợt 4: Đợt thanh toán/Công nợ dự án/Phát sinh, Đợt 5: Báo cáo dự án) + 1 đợt bổ sung ngoài kế hoạch (gộp tab Công việc vào tab Giai đoạn), tất cả đã test qua cả API lẫn trình duyệt thật, gần nhất ngày 2026-08-05. Không còn việc nào dang dở của module này.

**VIỆC TIẾP THEO ĐÃ CHỐT — Giao diện di động (2026-08-06)**: kế hoạch đã ghi đầy đủ vào `docs/PRD.md` (4.16), `docs/Plan.md` (mục 4 "Giao diện di động"), `docs/TASK.md`, `docs/DECISIONS.md`, và **Đợt 0 (thiết kế 10 thành phần) đã xong**, ghi vào `docs/DESIGN-SYSTEM.md` mục "Giao diện di động". **Vẫn chưa viết dòng code nào.** Bước kế tiếp là **Đợt 1 — khung app**: tách `frontend/assets/tokens.css` từ `style.css`, viết `frontend/m/assets/m-tokens.css`/`m-style.css`/`m-layout.js` (app bar + tab bar)/`m-ui.js` (bottom sheet, pull-to-refresh, skeleton), thêm phát hiện thiết bị vào `layout.js`/`auth.js`, `frontend/m/login.html` + `index.html`, `manifest.json`. Lưu ý khi làm: bản mobile nằm hẳn ở `frontend/m/`, **không sửa frontend desktop** ngoài đúng 3 chỗ đã lên kế hoạch (tách `tokens.css`, phát hiện thiết bị trong `layout.js`/`auth.js`, thêm icon mới vào `icons.js`); nghiệm thu bắt buộc **cả CDP touch emulation lẫn điện thoại thật qua LAN**; 2 hạng mục đụng backend (`rolling`/`maxAge` session và đổi `MemoryStore` sang store lưu đĩa) **cần duyệt riêng** trước khi làm.

Các hướng khác đang để ngỏ (chưa có yêu cầu cụ thể): hoàn tất Phase 5 trên máy chủ thật (IP tĩnh, PM2 startup, go-live), in phiếu nhập kho, mở rộng báo cáo, module Bán hàng/POS.

**Cần theo dõi tiếp (2026-08-06)**: đã sửa 1 lỗi tiềm ẩn có thể gây "đơ" (vòng lặp Gantt không giới hạn khi giai đoạn bị nhập nhầm năm), nhưng **chưa xác nhận chắc chắn** đây có phải nguyên nhân của lần người dùng báo cáo bị đơ hay không. Đã bổ sung hạ tầng ghi log (`data/logs/server-*.log` — request chậm ≥2s, lỗi JS trình duyệt qua `POST /api/client-logs`, `uncaughtException`/`unhandledRejection`) — nếu còn tái diễn, kiểm tra file log đúng ngày xảy ra sự cố trước tiên thay vì đoán. Cũng phát hiện `backend/server.js` dùng `express-session` `MemoryStore` mặc định — không khuyến cáo cho production (rò rỉ bộ nhớ dần), nên xem lại khi làm Phase 5 Go-live (cân nhắc `connect-sqlite3`/`session-file-store` hoặc store khác phù hợp deploy LAN offline).

**Ghi chú nhỏ (2026-08-01)**: cảnh báo vượt hạn mức công nợ trên trang Công nợ khách hàng chưa được test với dữ liệu thật có số dư vượt hạn mức (dữ liệu test hiện tại số dư khách hàng đều = 0) — logic đã review kỹ, nên test lại khi có dữ liệu thật vượt ngưỡng.
